/**
 * DevExtreme (cjs/__internal/grids/grid_core/ai_assistant/ai_assistant_integration_controller.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.AIAssistantIntegrationController = void 0;
var _ui = _interopRequireDefault(require("../../../../ui/widget/ui.errors"));
var _m_modules = require("../m_modules");

function _interopRequireDefault(e) {
    return e && e.__esModule ? e : {
        default: e
    }
}
class AIAssistantIntegrationController extends _m_modules.Controller {
    getAICommandCallbacks(callbacks) {
        return {
            onComplete: finalResponse => {
                var _callbacks$onComplete;
                if (!this.isRequestAwaitingCompletion()) {
                    return
                }
                this.processCommandCompletion();
                null === callbacks || void 0 === callbacks || null === (_callbacks$onComplete = callbacks.onComplete) || void 0 === _callbacks$onComplete || _callbacks$onComplete.call(callbacks, finalResponse)
            },
            onError: error => {
                var _callbacks$onError;
                this.processCommandCompletion();
                null === callbacks || void 0 === callbacks || null === (_callbacks$onError = callbacks.onError) || void 0 === _callbacks$onError || _callbacks$onError.call(callbacks, error)
            }
        }
    }
    processCommandCompletion() {
        this.abort = void 0
    }
    buildContext() {
        var _this$dataController$;
        return {
            keyExpr: this.option("keyExpr") ?? (null === (_this$dataController$ = this.dataController.getDataSource()) || void 0 === _this$dataController$ || null === (_this$dataController$ = _this$dataController$.store()) || void 0 === _this$dataController$ ? void 0 : _this$dataController$.key()),
            columns: this.buildColumnsContext(),
            filtering: {
                filterValue: this.option("filterValue")
            },
            paging: {
                pageIndex: this.dataController.pageIndex(),
                pageSize: this.dataController.pageSize(),
                totalCount: this.dataController.totalCount(),
                visibleRowCount: this.dataController.getVisibleRows().filter(row => "data" === row.rowType).length
            },
            search: {
                searchText: this.option("searchPanel.text") ?? ""
            },
            selection: {
                selectedRowKeys: this.option("selectedRowKeys") ?? [],
                mode: this.option("selection.mode"),
                selectAllMode: this.option("selection.selectAllMode")
            }
        }
    }
    buildColumnsContext() {
        const allColumns = this.columnsController.getColumns();
        return allColumns.filter(column => !column.command).map(column => this.buildColumnContext(column))
    }
    buildColumnContext(column) {
        return {
            dataField: column.dataField,
            caption: column.caption,
            dataType: column.dataType,
            visible: false !== column.visible,
            sortOrder: column.sortOrder,
            sortIndex: column.sortIndex,
            fixed: column.fixed,
            fixedPosition: column.fixedPosition,
            width: column.width,
            visibleIndex: column.visibleIndex
        }
    }
    init() {
        this.createAction("onAIAssistantRequestCreating");
        this.dataController = this.getController("data");
        this.columnsController = this.getController("columns")
    }
    getAIIntegration() {
        const assistantAIIntegration = this.option("aiAssistant.aiIntegration");
        if (assistantAIIntegration) {
            return assistantAIIntegration
        }
        const gridAIIntegration = this.option("aiIntegration");
        if (gridAIIntegration) {
            return gridAIIntegration
        }
        return null
    }
    sendRequest(text, responseSchema, callbacks) {
        if (this.isRequestAwaitingCompletion()) {
            this.abortRequest()
        }
        const aiIntegration = this.getAIIntegration();
        if (null === aiIntegration) {
            var _callbacks$onError2;
            _ui.default.log("E1068");
            null === callbacks || void 0 === callbacks || null === (_callbacks$onError2 = callbacks.onError) || void 0 === _callbacks$onError2 || _callbacks$onError2.call(callbacks, _ui.default.Error("E1068"));
            return
        }
        const args = {
            responseSchema: responseSchema,
            context: this.buildContext(),
            cancel: false,
            additionalInfo: {}
        };
        this.executeAction("onAIAssistantRequestCreating", args);
        if (args.cancel) {
            var _callbacks$onAbort;
            null === callbacks || void 0 === callbacks || null === (_callbacks$onAbort = callbacks.onAbort) || void 0 === _callbacks$onAbort || _callbacks$onAbort.call(callbacks);
            return
        }
        const abortRequest = aiIntegration.executeGridAssistant({
            text: text,
            context: args.context,
            responseSchema: args.responseSchema,
            additionalInfo: args.additionalInfo
        }, this.getAICommandCallbacks(callbacks));
        this.abort = () => {
            var _callbacks$onAbort2;
            abortRequest();
            null === callbacks || void 0 === callbacks || null === (_callbacks$onAbort2 = callbacks.onAbort) || void 0 === _callbacks$onAbort2 || _callbacks$onAbort2.call(callbacks)
        }
    }
    isRequestAwaitingCompletion() {
        return !!this.abort
    }
    abortRequest() {
        var _this$abort;
        null === (_this$abort = this.abort) || void 0 === _this$abort || _this$abort.call(this);
        this.abort = void 0
    }
    dispose() {
        super.dispose();
        this.abortRequest()
    }
}
exports.AIAssistantIntegrationController = AIAssistantIntegrationController;
