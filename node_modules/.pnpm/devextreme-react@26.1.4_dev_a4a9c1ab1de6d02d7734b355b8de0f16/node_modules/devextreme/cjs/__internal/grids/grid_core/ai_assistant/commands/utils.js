/**
 * DevExtreme (cjs/__internal/grids/grid_core/ai_assistant/commands/utils.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.isKeyShapeValid = exports.compositeKeyPairSchema = void 0;
exports.isMultiValueExpr = isMultiValueExpr;
exports.normalizeKey = void 0;
exports.optionalNullish = optionalNullish;
exports.pickKeysByIndex = void 0;
exports.resolveFilterValue = resolveFilterValue;
exports.splitIntoLoadWindows = void 0;
var _type = require("../../../../../core/utils/type");
var _date = require("../../../../core/utils/date");
var _m_utils = require("../../../../grids/grid_core/m_utils");
var _zod = require("zod");
const compositeKeyPairSchema = exports.compositeKeyPairSchema = _zod.z.object({
    field: _zod.z.string(),
    value: _zod.z.union([_zod.z.string(), _zod.z.number()])
}).strict();
const compositeKeyToObject = pairs => {
    const result = {};
    for (const {
            field: field,
            value: value
        }
        of pairs) {
        result[field] = value
    }
    return result
};
const normalizeKey = key => {
    if (Array.isArray(key)) {
        return compositeKeyToObject(key)
    }
    return key
};
exports.normalizeKey = normalizeKey;

function optionalNullish(schema) {
    return schema.nullish().transform(value => value ?? void 0)
}
const isKeyShapeValid = (keyExpr, key) => {
    if ((0, _type.isString)(keyExpr)) {
        return "string" === typeof key || "number" === typeof key
    }
    if ("object" !== typeof key || null === key) {
        return false
    }
    return keyExpr.every(field => field in key)
};
exports.isKeyShapeValid = isKeyShapeValid;
const splitIntoLoadWindows = (indexes, maxWindowSize) => {
    const sorted = [...new Set(indexes)].sort((a, b) => a - b);
    const windows = [];
    sorted.forEach(index => {
        const current = windows.at(-1);
        if (current && index - current[0] + 1 <= maxWindowSize) {
            current.push(index)
        } else {
            windows.push([index])
        }
    });
    return windows
};
exports.splitIntoLoadWindows = splitIntoLoadWindows;
const pickKeysByIndex = (keys, indexes) => {
    const normalizedRowIndexes = indexes.map(index => index - 1);
    const allIndexesValid = normalizedRowIndexes.every(index => index < keys.length);
    if (!allIndexesValid) {
        return null
    }
    return normalizedRowIndexes.map(index => keys[index])
};
exports.pickKeysByIndex = pickKeysByIndex;

function resolveFilterValue(dataType, value) {
    if ("string" === typeof value && (0, _m_utils.isDateType)(dataType)) {
        if (!_date.dateUtilsTs.isValidDate(value)) {
            return value
        }
        return new Date(value)
    }
    return value
}

function isMultiValueExpr(expr) {
    return Array.isArray(expr.value)
}
