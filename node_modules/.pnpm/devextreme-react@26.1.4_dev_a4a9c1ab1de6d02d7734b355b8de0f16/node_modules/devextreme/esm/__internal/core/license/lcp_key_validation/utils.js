/**
 * DevExtreme (esm/__internal/core/license/lcp_key_validation/utils.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    base64ToBytes,
    bigIntFromBytes
} from "../byte_utils";
import {
    pad
} from "../pkcs1";
import {
    compareSignatures
} from "../rsa_bigint";
import {
    sha1
} from "../sha1";
import {
    DECODE_MAP
} from "./const";
export const bit = shift => BigInt(1) << BigInt(shift);
export const parseRsaXml = xml => {
    const modulusMatch = /<Modulus>([^<]+)<\/Modulus>/.exec(xml);
    const exponentMatch = /<Exponent>([^<]+)<\/Exponent>/.exec(xml);
    if (!modulusMatch || !exponentMatch) {
        throw new Error("Invalid RSA XML key.")
    }
    return {
        modulus: base64ToBytes(modulusMatch[1]),
        exponent: Number(bigIntFromBytes(base64ToBytes(exponentMatch[1])))
    }
};
export const encodeString = (text, encode) => "function" === typeof encode ? encode(text) : text;
export const shiftText = (text, map) => {
    if (!text) {
        return text || ""
    }
    let result = "";
    for (let i = 0; i < text.length; i += 1) {
        const charCode = text.charCodeAt(i);
        if (charCode < map.length) {
            result += map[charCode]
        } else {
            result += text[i]
        }
    }
    return result
};
export const shiftDecodeText = text => shiftText(text, DECODE_MAP);
const DOT_NET_TICKS_EPOCH_OFFSET = BigInt("621355968000000000");
const DOT_NET_TICKS_PER_MS = BigInt(1e4);
const DOT_NET_MAX_VALUE_TICKS = BigInt("3155378975999999999");
export function dotNetTicksToMs(ticksStr) {
    const ticks = BigInt(ticksStr);
    if (ticks >= DOT_NET_MAX_VALUE_TICKS) {
        return 1 / 0
    }
    return Number((ticks - DOT_NET_TICKS_EPOCH_OFFSET) / DOT_NET_TICKS_PER_MS)
}
export const verifyHash = (xmlKey, data, signature) => {
    const {
        modulus: modulus,
        exponent: exponent
    } = parseRsaXml(xmlKey);
    const key = {
        n: modulus,
        e: exponent
    };
    const sign = base64ToBytes(signature);
    return compareSignatures({
        key: key,
        signature: sign,
        actual: pad(sha1(data))
    })
};
