/**
 * DevExtreme (cjs/__internal/scheduler/workspaces/view_model/view_data_generator_timeline_month.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.ViewDataGeneratorTimelineMonth = void 0;
var _date = _interopRequireDefault(require("../../../../core/utils/date"));
var _index = require("../../../scheduler/r1/utils/index");
var _view_data_generator = require("./view_data_generator");

function _interopRequireDefault(e) {
    return e && e.__esModule ? e : {
        default: e
    }
}
const toMs = _date.default.dateToMilliseconds;
class ViewDataGeneratorTimelineMonth extends _view_data_generator.ViewDataGenerator {
    getSkippedDaysAnchorDay(firstDayOfWeekOption, startViewDate) {
        return startViewDate.getDay()
    }
    calculateEndDate(startDate, interval, endDayHour) {
        return (0, _index.setOptionHour)(startDate, endDayHour)
    }
    getInterval() {
        return toMs("day")
    }
    getCellCountInDay() {
        return 1
    }
    calculateStartViewDate(options) {
        return _index.timelineMonthUtils.calculateStartViewDate(options.currentDate, options.startDayHour, options.startDate, options.intervalCount)
    }
    getCellCount(options) {
        const {
            intervalCount: intervalCount
        } = options;
        const currentDate = new Date(options.currentDate);
        let cellCount = 0;
        for (let i = 1; i <= intervalCount; i += 1) {
            const monthDate = new Date(currentDate.getFullYear(), currentDate.getMonth() + i, 0);
            const daysInMonth = monthDate.getDate();
            for (let day = 1; day <= daysInMonth; day += 1) {
                const date = new Date(monthDate.getFullYear(), monthDate.getMonth(), day);
                if (!this.isDateSkipped(date)) {
                    cellCount += 1
                }
            }
        }
        return cellCount
    }
    setHiddenInterval() {
        this.hiddenInterval = 0
    }
}
exports.ViewDataGeneratorTimelineMonth = ViewDataGeneratorTimelineMonth;
