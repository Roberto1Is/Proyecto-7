/**
 * DevExtreme (esm/__internal/grids/grid_core/ai_assistant/ai_assistant_view.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    fromPromise
} from "../../../core/utils/m_deferred";
import {
    AIChat
} from "../ai_chat/ai_chat";
import {
    View
} from "../m_modules";
import {
    AI_ASSISTANT_POPUP_OFFSET,
    CLASSES
} from "./const";
import {
    createConfirmDialog,
    isChatOptions,
    isEnabledOption,
    isPopupOptions,
    isTitleOption,
    isUserMessage
} from "./utils";
export class AIAssistantView extends View {
    constructor() {
        super(...arguments);
        this.isHidingAfterConfirm = false
    }
    init() {
        this.columnHeadersView = this.getView("columnHeadersView");
        this.aiAssistantController = this.getController("aiAssistant");
        this.messageStore = this.aiAssistantController.getMessageStore();
        this.handleMessageStorePushContext = this.handleMessageStorePush.bind(this);
        this.unsubscribeMessageStorePush();
        this.subscribeMessageStorePush()
    }
    getAIChatConfig() {
        const popupOptions = this.getAIChatPopupOptions();
        const chatOptions = this.getAIChatOptions();
        return {
            container: this.element(),
            createComponent: this._createComponent.bind(this),
            onRegenerate: aiMessage => {
                this.executeRequest(aiMessage)
            },
            popupOptions: popupOptions,
            chatOptions: chatOptions
        }
    }
    getAIChatPopupOptions() {
        const position = {
            my: "right top",
            at: "right top",
            of: this.columnHeadersView.element(),
            collision: "fit",
            offset: `${-AI_ASSISTANT_POPUP_OFFSET} ${AI_ASSISTANT_POPUP_OFFSET}`,
            boundaryOffset: `${AI_ASSISTANT_POPUP_OFFSET} ${AI_ASSISTANT_POPUP_OFFSET}`
        };
        return Object.assign({
            title: this.option("aiAssistant.title") ?? "",
            position: position,
            onShowing: () => {
                var _this$visibilityChang;
                null === (_this$visibilityChang = this.visibilityChanged) || void 0 === _this$visibilityChang || _this$visibilityChang.fire(true)
            },
            onHidden: () => {
                var _this$visibilityChang2;
                null === (_this$visibilityChang2 = this.visibilityChanged) || void 0 === _this$visibilityChang2 || _this$visibilityChang2.fire(false)
            },
            onHiding: e => {
                if (this.isHidingAfterConfirm) {
                    this.isHidingAfterConfirm = false;
                    return
                }
                if (this.aiAssistantController.isProcessing()) {
                    e.cancel = true;
                    const confirmDialog = createConfirmDialog({
                        popupOptions: {
                            elementAttr: {
                                class: this.addWidgetPrefix(CLASSES.aiAssistantConfirmDialog)
                            }
                        }
                    });
                    confirmDialog.show().done(confirmResult => {
                        if (confirmResult) {
                            this.aiAssistantController.abortRequest();
                            this.isHidingAfterConfirm = true;
                            e.component.hide()
                        }
                    })
                }
            }
        }, this.option("aiAssistant.popup"))
    }
    executeRequest(message) {
        var _this$aiChatInstance;
        null === (_this$aiChatInstance = this.aiChatInstance) || void 0 === _this$aiChatInstance || _this$aiChatInstance.setDisabled(true);
        fromPromise(this.aiAssistantController.sendRequestToAI(message)).always(() => {
            var _this$aiChatInstance2;
            null === (_this$aiChatInstance2 = this.aiChatInstance) || void 0 === _this$aiChatInstance2 || _this$aiChatInstance2.setDisabled(false)
        })
    }
    handleMessageStorePush(changes) {
        const userId = this.aiChatInstance.getUserId();
        changes.forEach(_ref => {
            let {
                type: type,
                data: data
            } = _ref;
            if ("insert" === type && data && isUserMessage(data, userId)) {
                this.executeRequest(data)
            }
        })
    }
    unsubscribeMessageStorePush() {
        var _this$messageStore;
        null === (_this$messageStore = this.messageStore) || void 0 === _this$messageStore || _this$messageStore.off("push", this.handleMessageStorePushContext)
    }
    subscribeMessageStorePush() {
        var _this$messageStore2;
        null === (_this$messageStore2 = this.messageStore) || void 0 === _this$messageStore2 || _this$messageStore2.on("push", this.handleMessageStorePushContext)
    }
    getAIChatOptions() {
        return Object.assign({
            dataSource: {
                store: this.messageStore,
                pushAggregationTimeout: 0
            },
            reloadOnChange: true,
            onMessageEntered: e => {
                this.executeRequest(e.message)
            }
        }, this.option("aiAssistant.chat"))
    }
    _renderCore() {
        if (!this.aiChatInstance) {
            const config = this.getAIChatConfig();
            this.aiChatInstance = new AIChat(config)
        }
    }
    dispose() {
        var _this$aiChatInstance3;
        this.unsubscribeMessageStorePush();
        this.messageStore = void 0;
        null === (_this$aiChatInstance3 = this.aiChatInstance) || void 0 === _this$aiChatInstance3 || _this$aiChatInstance3.dispose();
        super.dispose()
    }
    optionChanged(args) {
        if ("aiAssistant" === args.name) {
            const enabledChanged = isEnabledOption(args.fullName, args.value);
            if (enabledChanged) {
                if (this.isVisible()) {
                    this._invalidate()
                } else {
                    this.hide()
                }
            }
            const popupOptionsChanged = isTitleOption(args.fullName, args.value) || isPopupOptions(args.fullName, args.value);
            const chatOptionsChanged = isChatOptions(args.fullName, args.value);
            if (popupOptionsChanged || chatOptionsChanged) {
                var _this$aiChatInstance4;
                null === (_this$aiChatInstance4 = this.aiChatInstance) || void 0 === _this$aiChatInstance4 || _this$aiChatInstance4.updateOptions(this.getAIChatConfig(), popupOptionsChanged, chatOptionsChanged)
            }
            args.handled = true
        } else {
            super.optionChanged(args)
        }
    }
    callbackNames() {
        return ["visibilityChanged"]
    }
    isVisible() {
        return !!this.option("aiAssistant.enabled")
    }
    isShown() {
        var _this$aiChatInstance5;
        return (null === (_this$aiChatInstance5 = this.aiChatInstance) || void 0 === _this$aiChatInstance5 ? void 0 : _this$aiChatInstance5.isShown()) ?? false
    }
    hide() {
        var _this$aiChatInstance6;
        return (null === (_this$aiChatInstance6 = this.aiChatInstance) || void 0 === _this$aiChatInstance6 ? void 0 : _this$aiChatInstance6.hide()) ?? Promise.resolve(false)
    }
    toggle() {
        var _this$aiChatInstance7;
        return (null === (_this$aiChatInstance7 = this.aiChatInstance) || void 0 === _this$aiChatInstance7 ? void 0 : _this$aiChatInstance7.toggle()) ?? Promise.resolve(false)
    }
}
