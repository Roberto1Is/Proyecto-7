/**
 * DevExtreme (esm/__internal/scheduler/workspaces/view_model/view_data_generator_day.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    dayUtils
} from "../../r1/utils/index";
import {
    getFirstVisibleDate
} from "../../utils/skipped_days";
import {
    ViewDataGenerator
} from "./view_data_generator";
export class ViewDataGeneratorDay extends ViewDataGenerator {
    getSkippedDaysAnchorDay(firstDayOfWeekOption, startViewDate) {
        return startViewDate.getDay()
    }
    calculateStartViewDate(options) {
        const startViewDate = dayUtils.calculateStartViewDate(options.currentDate, options.startDayHour, options.startDate, this._getIntervalDuration(options.intervalCount));
        return getFirstVisibleDate(startViewDate, options.skippedDays ?? this.skippedDays, date => {
            const nextDate = new Date(date);
            nextDate.setDate(nextDate.getDate() + 1);
            return nextDate
        })
    }
}
