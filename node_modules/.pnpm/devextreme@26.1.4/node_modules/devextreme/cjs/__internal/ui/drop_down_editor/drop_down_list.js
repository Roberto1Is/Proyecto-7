/**
 * DevExtreme (cjs/__internal/ui/drop_down_editor/drop_down_list.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = void 0;
var _events_engine = _interopRequireDefault(require("../../../common/core/events/core/events_engine"));
var _utils = require("../../../common/core/events/utils");
var _message = _interopRequireDefault(require("../../../common/core/localization/message"));
var _query = _interopRequireDefault(require("../../../common/data/query"));
var _component_registrator = _interopRequireDefault(require("../../../core/component_registrator"));
var _devices = _interopRequireDefault(require("../../../core/devices"));
var _guid = _interopRequireDefault(require("../../../core/guid"));
var _renderer = _interopRequireDefault(require("../../../core/renderer"));
var _child_default_template = require("../../../core/templates/child_default_template");
var _common = require("../../../core/utils/common");
var _deferred = require("../../../core/utils/deferred");
var _extend = require("../../../core/utils/extend");
var _size = require("../../../core/utils/size");
var _type = require("../../../core/utils/type");
var _window = require("../../../core/utils/window");
var _ui = _interopRequireDefault(require("../../../ui/editor/ui.data_expression"));
var _ui2 = _interopRequireDefault(require("../../../ui/widget/ui.errors"));
var _grouped = require("../../data/data_converter/grouped");
var _drop_down_editor = _interopRequireDefault(require("../../ui/drop_down_editor/drop_down_editor"));
var _listEdit = _interopRequireDefault(require("../../ui/list/list.edit.search"));

function _interopRequireDefault(e) {
    return e && e.__esModule ? e : {
        default: e
    }
}
const window = (0, _window.getWindow)();
const LIST_ITEM_SELECTOR = ".dx-list-item";
const LIST_ITEM_DATA_KEY = "dxListItemData";
const DROPDOWNLIST_POPUP_WRAPPER_CLASS = "dx-dropdownlist-popup-wrapper";
const SEARCH_EVENT = "input";
const SEARCH_MODES = ["startswith", "contains", "endwith", "notcontains"];
const useCompositionEvents = "android" !== _devices.default.real().platform;
class DropDownList extends _drop_down_editor.default {
    _supportedKeys() {
        const parentSupportedKeys = super._supportedKeys();
        return Object.assign({}, parentSupportedKeys, {
            tab(e) {
                if (this._allowSelectItemByTab()) {
                    this._saveValueChangeEvent(e);
                    const {
                        focusedElement: focusedElement
                    } = this._list.option();
                    const $focusedItem = (0, _renderer.default)(focusedElement);
                    if ($focusedItem.length) {
                        this._setSelectedElement($focusedItem)
                    }
                }
                parentSupportedKeys.tab(e)
            },
            space: _common.noop,
            home: _common.noop,
            end: _common.noop
        })
    }
    _allowSelectItemByTab() {
        const {
            opened: opened,
            applyValueMode: applyValueMode
        } = this.option();
        return opened && "instantly" === applyValueMode
    }
    _setSelectedElement($element) {
        const value = this._valueGetter(this._list._getItemData($element));
        this._setValue(value)
    }
    _setValue(value) {
        this.option("value", value)
    }
    _getDefaultOptions() {
        return Object.assign({}, super._getDefaultOptions(), _ui.default._dataExpressionDefaultOptions(), {
            displayValue: void 0,
            searchEnabled: false,
            searchMode: "contains",
            searchTimeout: 500,
            minSearchLength: 0,
            searchExpr: null,
            valueChangeEvent: "input change keyup",
            selectedItem: null,
            noDataText: _message.default.format("dxCollectionWidget-noDataText"),
            encodeNoDataText: false,
            onSelectionChanged: null,
            onItemClick: _common.noop,
            showDataBeforeSearch: false,
            grouped: false,
            groupTemplate: "group",
            popupPosition: {
                my: "left top",
                at: "left bottom",
                offset: {
                    h: 0,
                    v: 0
                },
                collision: "flip"
            },
            wrapItemText: false,
            useItemTextAsTitle: false
        })
    }
    _defaultOptionsRules() {
        return super._defaultOptionsRules().concat([{
            device: {
                platform: "ios"
            },
            options: {
                popupPosition: {
                    offset: {
                        v: -1
                    }
                }
            }
        }, {
            device: {
                platform: "generic"
            },
            options: {
                buttonsLocation: "bottom center"
            }
        }])
    }
    _setOptionsByReference() {
        super._setOptionsByReference();
        (0, _extend.extend)(this._optionsByReference, {
            value: true,
            selectedItem: true,
            displayValue: true
        })
    }
    _init() {
        super._init();
        this._initDataExpressions();
        this._initActions();
        this._setListDataSource();
        this._validateSearchMode();
        this._clearSelectedItem();
        this._initItems()
    }
    _setListFocusedElementOptionChange() {
        if (!this._list) {
            return
        }
        this._list._updateParentActiveDescendant = this._updateActiveDescendant.bind(this)
    }
    _initItems() {
        const {
            items: items
        } = this.option();
        if (items && !items.length && this._dataSource) {
            this.option().items = this._dataSource.items()
        }
    }
    _initActions() {
        this._initContentReadyAction();
        this._initSelectionChangedAction();
        this._initItemClickAction()
    }
    _initContentReadyAction() {
        this._contentReadyAction = this._createActionByOption("onContentReady", {
            excludeValidators: ["disabled", "readOnly"]
        })
    }
    _initSelectionChangedAction() {
        this._selectionChangedAction = this._createActionByOption("onSelectionChanged", {
            excludeValidators: ["disabled", "readOnly"]
        })
    }
    _initItemClickAction() {
        this._itemClickAction = this._createActionByOption("onItemClick")
    }
    _initTemplates() {
        super._initTemplates();
        this._templateManager.addDefaultTemplates({
            item: new _child_default_template.ChildDefaultTemplate("item")
        })
    }
    _isEditable() {
        const {
            searchEnabled: searchEnabled
        } = this.option();
        return super._isEditable() || searchEnabled
    }
    _saveFocusOnWidget() {
        var _this$_list;
        if (null !== (_this$_list = this._list) && void 0 !== _this$_list && _this$_list.initialOption("focusStateEnabled")) {
            this._focusInput()
        }
    }
    _fitIntoRange(value, start, end) {
        if (value > end) {
            return start
        }
        if (value < start) {
            return end
        }
        return value
    }
    _items() {
        const items = this._getPlainItems(!this._list && this._dataSource.items());
        return new _query.default(items).filter("disabled", "<>", true).toArray()
    }
    _calcNextItem(step) {
        const items = this._items();
        const nextIndex = this._fitIntoRange(this._getSelectedIndex() + step, 0, items.length - 1);
        return items[nextIndex]
    }
    _getSelectedIndex() {
        const items = this._items();
        const {
            selectedItem: selectedItem
        } = this.option();
        return items.findIndex(item => this._isValueEquals(item, selectedItem))
    }
    _createPopup() {
        var _this$_popup, _this$_popup2;
        super._createPopup();
        this._updateCustomBoundaryContainer();
        null === (_this$_popup = this._popup) || void 0 === _this$_popup || null === (_this$_popup = _this$_popup.$wrapper()) || void 0 === _this$_popup || _this$_popup.addClass(this._popupWrapperClass());
        const $popupContent = null === (_this$_popup2 = this._popup) || void 0 === _this$_popup2 ? void 0 : _this$_popup2.$content();
        _events_engine.default.off($popupContent, "mouseup");
        _events_engine.default.on($popupContent, "mouseup", this._saveFocusOnWidget.bind(this))
    }
    _updateCustomBoundaryContainer() {
        const {
            dropDownOptions: dropDownOptions
        } = this.option();
        const customContainer = null === dropDownOptions || void 0 === dropDownOptions ? void 0 : dropDownOptions.container;
        const $container = (0, _renderer.default)(customContainer);
        if ($container.length && !(0, _type.isWindow)($container.get(0))) {
            const $containerWithParents = [].slice.call($container.parents());
            $containerWithParents.unshift($container.get(0));
            const overflowParent = $containerWithParents.find(parent => parent !== (0, _renderer.default)("body").get(0) && "hidden" === window.getComputedStyle(parent).overflowY);
            if (overflowParent) {
                this._$customBoundaryContainer = (0, _renderer.default)(overflowParent)
            }
        }
    }
    _popupWrapperClass() {
        return "dx-dropdownlist-popup-wrapper"
    }
    _renderInputValue() {
        var _this = this;
        let {
            value: value,
            renderOnly: renderOnly
        } = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : {};
        const currentValue = value ?? this._getCurrentValue();
        this._rejectValueLoading();
        if (renderOnly) {
            return super._renderInputValue(currentValue)
        }
        return this._loadInputValue(currentValue, function() {
            _this._setSelectedItem(...arguments)
        }).always(super._renderInputValue.bind(this, currentValue))
    }
    _loadInputValue(value, callback) {
        return this._loadItem(value).always(callback)
    }
    _getItemFromPlain(value, cache) {
        let plainItems = [];
        let selectedItem;
        if (cache && "object" !== typeof value) {
            if (!cache.itemByValue) {
                cache.itemByValue = {};
                plainItems = this._getPlainItems();
                const {
                    itemByValue: itemByValue
                } = cache;
                plainItems.forEach(item => {
                    itemByValue[this._valueGetter(item)] = item
                }, this)
            }
            selectedItem = cache.itemByValue[value]
        }
        if (!selectedItem) {
            plainItems = this._getPlainItems();
            selectedItem = (0, _common.grep)(plainItems, item => this._isValueEquals(this._valueGetter(item), value))[0]
        }
        return selectedItem
    }
    _resetInputText() {
        this._renderInputValue({
            renderOnly: true
        })
    }
    _loadItem(value, cache) {
        const selectedItem = this._getItemFromPlain(value, cache);
        if (void 0 !== selectedItem) {
            return (0, _deferred.Deferred)().resolve(selectedItem)
        }
        return this._loadValue(value)
    }
    _getPlainItems(inputItems) {
        const {
            grouped: grouped,
            items: optionItems
        } = this.option();
        const items = (Array.isArray(inputItems) ? inputItems : void 0) ?? optionItems ?? this._dataSource.items() ?? [];
        return items.flatMap(item => {
            const groupedItem = item;
            return grouped && groupedItem.items ? groupedItem.items : [item]
        })
    }
    _updateActiveDescendant($target) {
        var _this$_list2;
        const {
            opened: opened
        } = this.option();
        const listFocusedItemId = null === (_this$_list2 = this._list) || void 0 === _this$_list2 ? void 0 : _this$_list2.getFocusedItemId();
        const isElementOnDom = (0, _renderer.default)(`#${listFocusedItemId}`).length > 0;
        const activedescendant = opened && isElementOnDom && listFocusedItemId;
        this.setAria({
            activedescendant: activedescendant || null
        }, $target)
    }
    _setSelectedItem(item) {
        const displayValue = this._displayValue(item);
        this.option("selectedItem", (0, _common.ensureDefined)(item, null));
        this.option("displayValue", displayValue)
    }
    _displayValue(item) {
        return this._displayGetter(item)
    }
    _refreshSelected() {
        const cache = {};
        const elements = Array.from(this._listItemElements());
        elements.forEach(itemElement => {
            const $itemElement = (0, _renderer.default)(itemElement);
            const itemValue = this._valueGetter($itemElement.data("dxListItemData"));
            const isItemSelected = this._isSelectedValue(itemValue, cache);
            if (isItemSelected) {
                var _this$_list3;
                null === (_this$_list3 = this._list) || void 0 === _this$_list3 || _this$_list3.selectItem(itemElement)
            } else {
                var _this$_list4;
                null === (_this$_list4 = this._list) || void 0 === _this$_list4 || _this$_list4.unselectItem(itemElement)
            }
        })
    }
    _popupShownHandler() {
        super._popupShownHandler();
        this._setFocusPolicy()
    }
    _setFocusPolicy() {
        const {
            focusStateEnabled: focusStateEnabled
        } = this.option();
        if (!focusStateEnabled || !this._list) {
            return
        }
        this._list.option("focusedElement", null)
    }
    _isSelectedValue(value, cache) {
        const {
            value: optionValue
        } = this.option();
        return this._isValueEquals(value, optionValue)
    }
    _validateSearchMode() {
        const {
            searchMode: searchMode
        } = this.option();
        if (!searchMode) {
            return
        }
        const normalizedSearchMode = null === searchMode || void 0 === searchMode ? void 0 : searchMode.toLowerCase();
        if (!SEARCH_MODES.includes(normalizedSearchMode)) {
            throw _ui2.default.Error("E1019", searchMode)
        }
    }
    _clearSelectedItem() {
        this.option("selectedItem", null)
    }
    _processDataSourceChanging() {
        this._initDataController();
        this._setListOption("_dataController", this._dataController);
        this._setListDataSource();
        this._renderInputValue().fail(() => {
            if (this._isCustomValueAllowed()) {
                return
            }
            this._clearSelectedItem()
        })
    }
    _isCustomValueAllowed() {
        const {
            displayCustomValue: displayCustomValue
        } = this.option();
        return Boolean(displayCustomValue)
    }
    clear() {
        super.clear();
        this._clearFilter();
        this._clearSelectedItem()
    }
    _listItemElements() {
        return this._$list ? this._$list.find(".dx-list-item") : (0, _renderer.default)()
    }
    _popupConfig() {
        this._updateCustomBoundaryContainer();
        const maxHeight = this._getMaxHeight();
        return Object.assign({}, super._popupConfig(), {
            templatesRenderAsynchronously: false,
            autoResizeEnabled: false,
            maxHeight: maxHeight
        })
    }
    _renderPopupContent() {
        super._renderPopupContent();
        this._renderList()
    }
    _getKeyboardListeners() {
        const canListHaveFocus = this._canListHaveFocus();
        if (!canListHaveFocus) {
            return super._getKeyboardListeners().concat([this._list])
        }
        return super._getKeyboardListeners()
    }
    _renderList() {
        var _this$_popup3;
        this._listId = `dx-${(new _guid.default)._value}`;
        const $list = (0, _renderer.default)("<div>").attr("id", this._listId).appendTo(null === (_this$_popup3 = this._popup) || void 0 === _this$_popup3 ? void 0 : _this$_popup3.$content());
        this._$list = $list;
        this._list = this._createComponent($list, _listEdit.default, this._listConfig());
        this._refreshList();
        this._renderPreventBlurOnListClick();
        this._setListFocusedElementOptionChange()
    }
    _renderPreventBlurOnListClick() {
        const eventName = (0, _utils.addNamespace)("mousedown", "dxDropDownList");
        _events_engine.default.off(this._$list, eventName);
        _events_engine.default.on(this._$list, eventName, e => e.preventDefault())
    }
    _getControlsAria() {
        return this._list && this._listId
    }
    _renderOpenedState() {
        super._renderOpenedState();
        if (this._list) {
            this._updateActiveDescendant()
        }
        this.setAria("owns", this._popup && this._popupContentId)
    }
    _getAriaHasPopup() {
        return "listbox"
    }
    _refreshList() {
        if (this._list && this._shouldRefreshDataSource()) {
            this._setListDataSource()
        }
    }
    _shouldRefreshDataSource() {
        var _this$_list5;
        const {
            dataSource: dataSource
        } = (null === (_this$_list5 = this._list) || void 0 === _this$_list5 ? void 0 : _this$_list5.option()) ?? {};
        return Boolean(dataSource) !== this._needPassDataSourceToList()
    }
    _isDesktopDevice() {
        return "desktop" === _devices.default.real().deviceType
    }
    _listConfig() {
        const {
            noDataText: noDataText,
            grouped: grouped,
            wrapItemText: wrapItemText,
            itemTemplate: itemTemplate,
            groupTemplate: groupTemplate,
            hoverStateEnabled: hoverStateEnabled,
            focusStateEnabled: focusStateEnabled,
            encodeNoDataText: encodeNoDataText,
            useItemTextAsTitle: useItemTextAsTitle
        } = this.option();
        const options = {
            selectionMode: "single",
            _templates: this.option("_templates"),
            templateProvider: this.option("templateProvider"),
            noDataText: noDataText,
            encodeNoDataText: encodeNoDataText,
            grouped: grouped,
            wrapItemText: wrapItemText,
            useItemTextAsTitle: useItemTextAsTitle,
            onContentReady: this._listContentReadyHandler.bind(this),
            itemTemplate: itemTemplate,
            indicateLoading: false,
            keyExpr: this._getCollectionKeyExpr(),
            displayExpr: this._displayGetterExpr(),
            groupTemplate: groupTemplate,
            onItemClick: this._listItemClickAction.bind(this),
            dataSource: this._getDataSource(),
            _dataController: this._dataController,
            hoverStateEnabled: this._isDesktopDevice() ? hoverStateEnabled : false,
            focusStateEnabled: focusStateEnabled,
            _onItemsRendered: () => {
                var _this$_popup4;
                null === (_this$_popup4 = this._popup) || void 0 === _this$_popup4 || _this$_popup4.repaint()
            }
        };
        if (!this._canListHaveFocus()) {
            options.tabIndex = null
        }
        return options
    }
    _canListHaveFocus() {
        return false
    }
    _getDataSource() {
        return this._needPassDataSourceToList() ? this._dataSource : null
    }
    _dataSourceOptions() {
        return {
            paginate: false
        }
    }
    _getSpecificDataSourceOption() {
        const {
            grouped: grouped,
            dataSource: dataSource
        } = this.option();
        if (dataSource && grouped) {
            return (0, _grouped.getDataSourceOptions)(dataSource)
        }
        return dataSource
    }
    _dataSourceFromUrlLoadMode() {
        return "raw"
    }
    _listContentReadyHandler() {
        var _this$_contentReadyAc;
        if (!this._$list) {
            return
        }
        const {
            deferRendering: deferRendering
        } = this.option();
        this._list = _listEdit.default.getInstance(this._$list);
        if (!deferRendering) {
            this._refreshSelected()
        }
        this._updatePopupWidth();
        this._updateListDimensions();
        null === (_this$_contentReadyAc = this._contentReadyAction) || void 0 === _this$_contentReadyAc || _this$_contentReadyAc.call(this)
    }
    _setListOption() {
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
            args[_key] = arguments[_key]
        }
        this._setWidgetOption("_list", args)
    }
    _listItemClickAction(e) {
        this._listItemClickHandler(e);
        this._itemClickAction(e)
    }
    _listItemClickHandler(e) {}
    _setListDataSource() {
        if (!this._list) {
            return
        }
        this._setListOption("dataSource", this._getDataSource());
        if (!this._needPassDataSourceToList()) {
            this._setListOption("items", [])
        }
    }
    _needPassDataSourceToList() {
        const {
            showDataBeforeSearch: showDataBeforeSearch
        } = this.option();
        return showDataBeforeSearch || this._isMinSearchLengthExceeded()
    }
    _isMinSearchLengthExceeded() {
        const {
            minSearchLength: minSearchLength
        } = this.option();
        return this._searchValue().toString().length >= (minSearchLength ?? 0)
    }
    _needClearFilter() {
        return this._canKeepDataSource() ? false : this._needPassDataSourceToList()
    }
    _canKeepDataSource() {
        const isMinSearchLengthExceeded = this._isMinSearchLengthExceeded();
        const {
            showDataBeforeSearch: showDataBeforeSearch,
            minSearchLength: minSearchLength
        } = this.option();
        return this._dataController.isLoaded() && Boolean(showDataBeforeSearch) && Boolean(minSearchLength) && !isMinSearchLengthExceeded && !this._isLastMinSearchLengthExceeded
    }
    _searchValue() {
        return this._input().val() || ""
    }
    _getSearchEvent() {
        return (0, _utils.addNamespace)("input", `${this.NAME}Search`)
    }
    _getCompositionStartEvent() {
        return (0, _utils.addNamespace)("compositionstart", `${this.NAME}CompositionStart`)
    }
    _getCompositionEndEvent() {
        return (0, _utils.addNamespace)("compositionend", `${this.NAME}CompositionEnd`)
    }
    _getSetFocusPolicyEvent() {
        return (0, _utils.addNamespace)("input", `${this.NAME}FocusPolicy`)
    }
    _renderEvents() {
        super._renderEvents();
        _events_engine.default.on(this._input(), this._getSetFocusPolicyEvent(), () => {
            this._setFocusPolicy()
        });
        if (this._shouldRenderSearchEvent()) {
            _events_engine.default.on(this._input(), this._getSearchEvent(), e => {
                this._searchHandler(e)
            });
            if (useCompositionEvents) {
                _events_engine.default.on(this._input(), this._getCompositionStartEvent(), () => {
                    this._isTextCompositionInProgress(true)
                });
                _events_engine.default.on(this._input(), this._getCompositionEndEvent(), e => {
                    this._isTextCompositionInProgress(void 0);
                    this._searchHandler(e, this._searchValue())
                })
            }
        }
    }
    _shouldRenderSearchEvent() {
        const {
            searchEnabled: searchEnabled
        } = this.option();
        return searchEnabled
    }
    _refreshEvents() {
        _events_engine.default.off(this._input(), this._getSearchEvent());
        _events_engine.default.off(this._input(), this._getSetFocusPolicyEvent());
        if (useCompositionEvents) {
            _events_engine.default.off(this._input(), this._getCompositionStartEvent());
            _events_engine.default.off(this._input(), this._getCompositionEndEvent())
        }
        super._refreshEvents()
    }
    _isTextCompositionInProgress(value) {
        if (arguments.length) {
            this._isTextComposition = value
        } else {
            return this._isTextComposition
        }
        return
    }
    _searchHandler(e, searchValue) {
        if (this._isTextCompositionInProgress()) {
            return
        }
        if (!this._isMinSearchLengthExceeded()) {
            this._searchCanceled();
            return
        }
        const {
            searchTimeout: searchTimeout
        } = this.option();
        if (searchTimeout) {
            this._clearSearchTimer();
            this._searchTimer = setTimeout(() => {
                this._searchDataSource(searchValue)
            }, searchTimeout)
        } else {
            this._searchDataSource(searchValue)
        }
    }
    _searchCanceled() {
        this._clearSearchTimer();
        if (this._needClearFilter()) {
            this._filterDataSource(null)
        }
        this._refreshList()
    }
    _searchDataSource() {
        let searchValue = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : this._searchValue();
        this._filterDataSource(searchValue)
    }
    _filterDataSource(searchValue) {
        const {
            searchExpr: searchExpr
        } = this.option();
        this._clearSearchTimer();
        const dataController = this._dataController;
        dataController.searchExpr(searchExpr ?? this._displayGetterExpr());
        dataController.searchOperation(this.option("searchMode"));
        dataController.searchValue(searchValue);
        dataController.load().done(this._dataSourceFiltered.bind(this, searchValue))
    }
    _clearFilter() {
        const dataController = this._dataController;
        if (dataController.searchValue()) {
            dataController.searchValue(null)
        }
    }
    _dataSourceFiltered(searchValue) {
        this._isLastMinSearchLengthExceeded = this._isMinSearchLengthExceeded();
        this._refreshList();
        this._refreshPopupVisibility()
    }
    _shouldOpenPopup() {
        return this._hasItemsToShow()
    }
    _refreshPopupVisibility() {
        const {
            readOnly: readOnly
        } = this.option();
        if (readOnly || !this._searchValue()) {
            return
        }
        const shouldOpenPopup = this._shouldOpenPopup();
        if (shouldOpenPopup && !this._isFocused()) {
            return
        }
        this.option("opened", shouldOpenPopup);
        if (shouldOpenPopup) {
            this._updatePopupWidth();
            this._updateListDimensions()
        }
    }
    _dataSourceChangedHandler(newItems) {
        if (0 === this._dataController.pageIndex()) {
            this.option().items = newItems
        } else {
            this.option().items = (this.option().items ?? []).concat(newItems)
        }
    }
    _hasItemsToShow() {
        const dataController = this._dataController;
        const resultItems = dataController.items() ?? [];
        const resultAmount = resultItems.length;
        const isMinSearchLengthExceeded = this._needPassDataSourceToList();
        return !!(isMinSearchLengthExceeded && resultAmount)
    }
    _clearSearchTimer() {
        clearTimeout(this._searchTimer);
        delete this._searchTimer
    }
    _popupShowingHandler() {
        this._updatePopupWidth();
        this._updatePopupMaxHeight();
        this._updateListDimensions()
    }
    _updatePopupMaxHeight() {
        var _this$_options$cache;
        const cachedMaxHeight = null === (_this$_options$cache = this._options.cache("dropDownOptions")) || void 0 === _this$_options$cache ? void 0 : _this$_options$cache.maxHeight;
        if (void 0 === cachedMaxHeight) {
            const maxHeight = this._getMaxHeight();
            this._setPopupOption("maxHeight", maxHeight)
        }
    }
    _dimensionChanged() {
        super._dimensionChanged();
        this._updateListDimensions();
        this._updatePopupMaxHeight()
    }
    _needPopupRepaint() {
        var _this$_list6;
        const dataController = this._dataController;
        const currentPageIndex = dataController.pageIndex();
        const needRepaint = (0, _type.isDefined)(this._pageIndex) && currentPageIndex <= this._pageIndex || dataController.isLastPage() && !(null !== (_this$_list6 = this._list) && void 0 !== _this$_list6 && _this$_list6._scrollViewIsFull());
        this._pageIndex = currentPageIndex;
        return needRepaint
    }
    _updateListDimensions() {
        if (!this._popup) {
            return
        }
        if (this._needPopupRepaint()) {
            this._popup.repaint()
        }
        if (this._list) {
            this._list.updateDimensions()
        }
    }
    _getMaxHeight() {
        var _$element$offset, _$customBoundaryConta;
        const $element = this.$element();
        const $customBoundaryContainer = this._$customBoundaryContainer;
        const offsetTop = ((null === (_$element$offset = $element.offset()) || void 0 === _$element$offset ? void 0 : _$element$offset.top) ?? 0) - ($customBoundaryContainer ? (null === (_$customBoundaryConta = $customBoundaryContainer.offset()) || void 0 === _$customBoundaryConta ? void 0 : _$customBoundaryConta.top) ?? 0 : 0);
        const windowHeight = (0, _size.getOuterHeight)(window);
        const containerHeight = $customBoundaryContainer ? Math.min((0, _size.getOuterHeight)($customBoundaryContainer), windowHeight) : windowHeight;
        const maxHeight = Math.max(offsetTop, containerHeight - offsetTop - (0, _size.getOuterHeight)($element));
        return Math.min(.5 * containerHeight, maxHeight)
    }
    _clean() {
        if (this._list) {
            delete this._list
        }
        delete this._isLastMinSearchLengthExceeded;
        super._clean()
    }
    _dispose() {
        this._clearSearchTimer();
        super._dispose()
    }
    _setCollectionWidgetOption() {
        this._setListOption(...arguments)
    }
    _setSubmitValue() {
        const {
            value: value
        } = this.option();
        const submitValue = this._shouldUseDisplayValue(value) ? this._displayGetter(value) : value;
        this._getSubmitElement().val(submitValue)
    }
    _shouldUseDisplayValue(value) {
        const {
            valueExpr: valueExpr
        } = this.option();
        return "this" === valueExpr && (0, _type.isObject)(value)
    }
    _optionChanged(args) {
        this._dataExpressionOptionChanged(args);
        switch (args.name) {
            case "hoverStateEnabled":
                if (this._isDesktopDevice()) {
                    this._setListOption(args.name, args.value)
                }
                super._optionChanged(args);
                break;
            case "focusStateEnabled":
                this._setListOption(args.name, args.value);
                super._optionChanged(args);
                break;
            case "items":
                if (!this.option("dataSource")) {
                    this._processDataSourceChanging()
                }
                break;
            case "dataSource":
                this._processDataSourceChanging();
                break;
            case "valueExpr":
                this._renderValue();
                this._setListOption("keyExpr", this._getCollectionKeyExpr());
                break;
            case "displayExpr":
                this._renderValue();
                this._setListOption("displayExpr", this._displayGetterExpr());
                break;
            case "searchMode":
                this._validateSearchMode();
                break;
            case "minSearchLength":
                this._refreshList();
                break;
            case "searchEnabled":
            case "showDataBeforeSearch":
            case "searchExpr":
                this._invalidate();
                break;
            case "onContentReady":
                this._initContentReadyAction();
                break;
            case "onSelectionChanged":
                this._initSelectionChangedAction();
                break;
            case "onItemClick":
                this._initItemClickAction();
                break;
            case "grouped":
            case "groupTemplate":
            case "wrapItemText":
            case "noDataText":
            case "encodeNoDataText":
            case "useItemTextAsTitle":
                this._setListOption(args.name);
                break;
            case "displayValue":
                this.option("text", args.value);
                break;
            case "itemTemplate":
            case "searchTimeout":
                break;
            case "selectedItem":
                if (args.previousValue !== args.value) {
                    this._selectionChangedAction({
                        selectedItem: args.value
                    })
                }
                break;
            default:
                super._optionChanged(args)
        }
    }
}
DropDownList.include(_ui.default);
(0, _component_registrator.default)("dxDropDownList", DropDownList);
var _default = exports.default = DropDownList;
