/**
 * DevExtreme (cjs/__internal/ui/select_box.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = exports.SELECTBOX_CLASS = void 0;
require("../ui/list/modules/selection");
var _utils = require("../../common/core/events/utils");
var _message = _interopRequireDefault(require("../../common/core/localization/message"));
var _component_registrator = _interopRequireDefault(require("../../core/component_registrator"));
var _dom_adapter = _interopRequireDefault(require("../../core/dom_adapter"));
var _element = require("../../core/element");
var _errors = _interopRequireDefault(require("../../core/errors"));
var _renderer = _interopRequireDefault(require("../../core/renderer"));
var _common = require("../../core/utils/common");
var _deferred = require("../../core/utils/deferred");
var _extend = require("../../core/utils/extend");
var _type = require("../../core/utils/type");
var _drop_down_list = _interopRequireDefault(require("../ui/drop_down_editor/drop_down_list"));

function _interopRequireDefault(e) {
    return e && e.__esModule ? e : {
        default: e
    }
}
const DISABLED_STATE_SELECTOR = ".dx-state-disabled";
const SELECTBOX_CLASS = exports.SELECTBOX_CLASS = "dx-selectbox";
const SELECTBOX_POPUP_CLASS = "dx-selectbox-popup";
const SELECTBOX_CONTAINER_CLASS = "dx-selectbox-container";
const SELECTBOX_POPUP_WRAPPER_CLASS = "dx-selectbox-popup-wrapper";
class SelectBox extends _drop_down_list.default {
    _supportedKeys() {
        const parent = super._supportedKeys();
        const clearSelectBox = e => {
            const {
                showClearButton: showClearButton
            } = this.option();
            const isEditable = this._isEditable();
            if (!isEditable) {
                if (showClearButton) {
                    e.preventDefault();
                    this.clear()
                }
            } else if (this._valueSubstituted()) {
                this._preventFiltering = true
            }
            this._savedTextRemoveEvent = e;
            this._preventSubstitution = true
        };
        const searchIfNeeded = () => {
            const {
                searchEnabled: searchEnabled
            } = this.option();
            if (searchEnabled && this._valueSubstituted()) {
                this._searchHandler()
            }
        };
        return Object.assign({}, parent, {
            tab: e => {
                var _this$_popup, _parent$tab;
                const {
                    opened: opened
                } = this.option();
                const popupHasFocusableElements = opened && !!(null !== (_this$_popup = this._popup) && void 0 !== _this$_popup && _this$_popup.getFocusableElements().length);
                if (!popupHasFocusableElements) {
                    this._resetCaretPosition(true)
                }
                null === (_parent$tab = parent.tab) || void 0 === _parent$tab || _parent$tab.call(this, e);
                if (!popupHasFocusableElements) {
                    this._cancelSearchIfNeed()
                }
            },
            upArrow: e => {
                if (parent.upArrow.call(this, e)) {
                    const {
                        opened: opened
                    } = this.option();
                    if (!opened) {
                        this._setNextValue(e)
                    }
                    return true
                }
                return
            },
            downArrow: e => {
                if (parent.downArrow.call(this, e)) {
                    const {
                        opened: opened
                    } = this.option();
                    if (!opened) {
                        this._setNextValue(e)
                    }
                    return true
                }
                return
            },
            leftArrow: e => {
                var _parent$leftArrow;
                searchIfNeeded();
                null === (_parent$leftArrow = parent.leftArrow) || void 0 === _parent$leftArrow || _parent$leftArrow.call(this, e)
            },
            rightArrow: e => {
                var _parent$rightArrow;
                searchIfNeeded();
                null === (_parent$rightArrow = parent.rightArrow) || void 0 === _parent$rightArrow || _parent$rightArrow.call(this, e)
            },
            home: e => {
                var _parent$home;
                searchIfNeeded();
                null === (_parent$home = parent.home) || void 0 === _parent$home || _parent$home.call(this, e)
            },
            end: e => {
                var _parent$end;
                searchIfNeeded();
                null === (_parent$end = parent.end) || void 0 === _parent$end || _parent$end.call(this, e)
            },
            escape: e => {
                var _parent$escape;
                const result = null === (_parent$escape = parent.escape) || void 0 === _parent$escape ? void 0 : _parent$escape.call(this, e);
                this._cancelEditing();
                return result ?? true
            },
            enter: e => {
                var _this$_list;
                const {
                    opened: opened,
                    value: value,
                    acceptCustomValue: acceptCustomValue,
                    allowClearing: allowClearing
                } = this.option();
                const inputText = this._input().val().trim();
                const {
                    focusedElement: focusedElement
                } = (null === (_this$_list = this._list) || void 0 === _this$_list ? void 0 : _this$_list.option()) ?? {};
                const isCustomText = inputText && this._list && !focusedElement;
                if (!inputText && (0, _type.isDefined)(value) && allowClearing) {
                    this._saveValueChangeEvent(e);
                    this.option({
                        selectedItem: null,
                        value: null
                    });
                    this.close()
                } else {
                    var _parent$enter;
                    if (acceptCustomValue) {
                        e.preventDefault();
                        if (isCustomText) {
                            if (opened) {
                                this._toggleOpenState()
                            }
                            this._valueChangeEventHandler(e)
                        }
                        return opened
                    }
                    if (null !== (_parent$enter = parent.enter) && void 0 !== _parent$enter && _parent$enter.call(this, e)) {
                        return opened
                    }
                }
                return
            },
            space: e => {
                const {
                    opened: opened,
                    searchEnabled: searchEnabled,
                    acceptCustomValue: acceptCustomValue
                } = this.option();
                if (!opened || searchEnabled || acceptCustomValue) {
                    return
                }
                e.preventDefault();
                this._valueChangeEventHandler(e);
                return true
            },
            backspace: clearSelectBox,
            del: clearSelectBox
        })
    }
    _getDefaultOptions() {
        return Object.assign({}, super._getDefaultOptions(), {
            placeholder: _message.default.format("Select"),
            fieldTemplate: null,
            customItemCreateEvent: "change",
            acceptCustomValue: false,
            onCustomItemCreating(e) {
                if (!(0, _type.isDefined)(e.customItem)) {
                    e.customItem = e.text
                }
            },
            showSelectionControls: false,
            allowClearing: true,
            tooltipEnabled: false,
            openOnFieldClick: true,
            showDropDownButton: true,
            displayCustomValue: false,
            useHiddenSubmitElement: true
        })
    }
    _init() {
        super._init();
        this._initCustomItemCreatingAction()
    }
    _initMarkup() {
        this.$element().addClass(SELECTBOX_CLASS);
        this._renderTooltip();
        super._initMarkup();
        this._$container.addClass("dx-selectbox-container")
    }
    _createPopup() {
        var _this$_popup2, _this$_popup3;
        super._createPopup();
        null === (_this$_popup2 = this._popup) || void 0 === _this$_popup2 || _this$_popup2.$element().addClass("dx-selectbox-popup");
        null === (_this$_popup3 = this._popup) || void 0 === _this$_popup3 || _this$_popup3.$overlayContent().attr("tabindex", -1)
    }
    _popupWrapperClass() {
        return `${super._popupWrapperClass()} dx-selectbox-popup-wrapper`
    }
    _cancelEditing() {
        const {
            searchEnabled: searchEnabled,
            selectedItem: selectedItem
        } = this.option();
        if (!searchEnabled && this._list) {
            this._focusListElement(null);
            this._updateField(selectedItem)
        }
    }
    _renderOpenedState() {
        super._renderOpenedState();
        const {
            opened: opened
        } = this.option();
        if (opened) {
            this._scrollToSelectedItem();
            this._focusSelectedElement()
        }
    }
    _focusSelectedElement() {
        var _this$_list2;
        const searchValue = this._searchValue();
        if (!searchValue) {
            this._focusListElement(null);
            return
        }
        const {
            items: items,
            selectedItem: selectedItem
        } = this.option();
        const $listItems = null === (_this$_list2 = this._list) || void 0 === _this$_list2 ? void 0 : _this$_list2._itemElements();
        const index = (null === items || void 0 === items ? void 0 : items.indexOf(selectedItem)) ?? -1;
        const focusedElement = -1 !== index && !this._isCustomItemSelected() ? null === $listItems || void 0 === $listItems ? void 0 : $listItems.eq(index) : null;
        this._focusListElement(focusedElement)
    }
    _renderFocusedElement() {
        if (!this._list) {
            return
        }
        const {
            acceptCustomValue: acceptCustomValue
        } = this.option();
        const searchValue = this._searchValue();
        if (!searchValue || acceptCustomValue) {
            this._focusListElement(null);
            return
        }
        const $listItems = this._list._itemElements();
        const focusedElement = $listItems.not(".dx-state-disabled").eq(0);
        this._focusListElement(focusedElement)
    }
    _focusListElement(element) {
        var _this$_list3;
        this._preventInputValueRender = true;
        null === (_this$_list3 = this._list) || void 0 === _this$_list3 || _this$_list3.option("focusedElement", element ? (0, _element.getPublicElement)(element) : null);
        delete this._preventInputValueRender
    }
    _scrollToSelectedItem() {
        if (!this._list) {
            return
        }
        const {
            selectedItem: selectedItem
        } = this._list.option();
        this._list.scrollToItem(selectedItem)
    }
    _listContentReadyHandler() {
        super._listContentReadyHandler();
        const isPaginate = this._dataController.paginate();
        if (isPaginate && this._needPopupRepaint()) {
            return
        }
        this._scrollToSelectedItem()
    }
    _renderValue() {
        this._renderInputValue();
        this._setSubmitValue();
        return (0, _deferred.Deferred)().resolve()
    }
    _renderInputValue() {
        let args = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : {};
        return super._renderInputValue(args).always(() => {
            this._renderInputValueAsync()
        })
    }
    _renderInputValueAsync() {
        this._renderTooltip();
        this._renderInputValueImpl().always(() => {
            this._refreshSelected()
        })
    }
    _renderInputValueImpl() {
        this._renderField();
        return (0, _deferred.Deferred)().resolve()
    }
    _setNextItem(step) {
        const item = this._calcNextItem(step);
        const value = this._valueGetter(item);
        this._setValue(value)
    }
    _setNextValue(e) {
        const dataSourceIsLoaded = this._dataController.isLoaded() ? (0, _deferred.Deferred)().resolve() : this._dataController.load();
        dataSourceIsLoaded.done(() => {
            const selectedIndex = this._getSelectedIndex();
            const hasPages = this._dataController.pageSize();
            const isLastPage = this._dataController.isLastPage();
            const isLastItem = selectedIndex === this._items().length - 1;
            this._saveValueChangeEvent(e);
            const step = "downArrow" === (0, _utils.normalizeKeyName)(e) ? 1 : -1;
            if (hasPages && !isLastPage && isLastItem && step > 0) {
                if (!this._popup) {
                    this._createPopup()
                }
                if (!this._dataController.isLoading()) {
                    var _this$_list4;
                    null === (_this$_list4 = this._list) || void 0 === _this$_list4 || _this$_list4._loadNextPage().done(this._setNextItem.bind(this, step))
                }
            } else {
                this._setNextItem(step)
            }
        })
    }
    _setSelectedItem(item) {
        const isUnknownItem = !this._isCustomValueAllowed() && void 0 === item;
        super._setSelectedItem(isUnknownItem ? null : item);
        if (!isUnknownItem && (!this._isEditable() || this._isCustomItemSelected())) {
            const {
                selectedItem: selectedItem
            } = this.option();
            this._setListOption("selectedItem", selectedItem)
        }
    }
    _isCustomValueAllowed() {
        const {
            acceptCustomValue: acceptCustomValue
        } = this.option();
        return Boolean(acceptCustomValue) || super._isCustomValueAllowed()
    }
    _displayValue(item) {
        const {
            value: value
        } = this.option();
        const normalizedItem = !(0, _type.isDefined)(item) && this._isCustomValueAllowed() ? value : item;
        return super._displayValue(normalizedItem)
    }
    _listConfig() {
        const {
            selectedItem: selectedItem,
            showSelectionControls: showSelectionControls
        } = this.option();
        const result = (0, _extend.extend)(super._listConfig(), {
            pageLoadMode: "scrollBottom",
            onSelectionChanged: this._getSelectionChangeHandler(),
            selectedItem: selectedItem,
            onFocusedItemChanged: this._listFocusedItemChangeHandler.bind(this),
            _onItemsRendered: () => {
                var _this$_popup4;
                null === (_this$_popup4 = this._popup) || void 0 === _this$_popup4 || _this$_popup4.repaint();
                if (this.option("opened")) {
                    this._scrollToSelectedItem()
                }
            }
        });
        return Object.assign({}, result, showSelectionControls && {
            showSelectionControls: true,
            selectByClick: true
        })
    }
    _listFocusedItemChangeHandler(e) {
        if (this._preventInputValueRender) {
            return
        }
        const list = e.component;
        const focusedElement = (0, _renderer.default)(list.option("focusedElement"));
        const focusedItem = list._getItemData(focusedElement);
        this._updateField(focusedItem)
    }
    _updateField(item) {
        const {
            fieldTemplate: fieldTemplateOption
        } = this.option();
        const fieldTemplate = this._getTemplate(fieldTemplateOption);
        if (!(fieldTemplate && fieldTemplateOption)) {
            const text = this._displayGetter(item);
            this.option("text", text);
            this._renderDisplayText(text);
            return
        }
        this._renderField()
    }
    _getSelectionChangeHandler() {
        const {
            showSelectionControls: showSelectionControls
        } = this.option();
        return showSelectionControls ? this._selectionChangeHandler.bind(this) : _common.noop
    }
    _selectionChangeHandler(e) {
        (e.addedItems ?? []).forEach(addedItem => {
            this._setValue(this._valueGetter(addedItem))
        })
    }
    _getActualSearchValue() {
        return this._dataController.searchValue()
    }
    _isInlineAutocompleteEnabled() {
        const {
            searchEnabled: searchEnabled,
            acceptCustomValue: acceptCustomValue,
            searchMode: searchMode
        } = this.option();
        return searchEnabled && !acceptCustomValue && "startswith" === searchMode
    }
    _getAriaAutocomplete() {
        const {
            disabled: disabled,
            readOnly: readOnly,
            searchEnabled: searchEnabled
        } = this.option();
        const isInputEditable = !(readOnly || disabled);
        const hasAutocomplete = searchEnabled && isInputEditable;
        if (!hasAutocomplete) {
            return "none"
        }
        const isInlineAutocompleteEnabled = this._isInlineAutocompleteEnabled();
        const autocompleteAria = isInlineAutocompleteEnabled ? "both" : "list";
        return autocompleteAria
    }
    _toggleOpenState(isVisible) {
        const {
            disabled: disabled,
            opened: opened,
            showDataBeforeSearch: showDataBeforeSearch
        } = this.option();
        if (disabled) {
            return
        }
        const resolvedVisible = arguments.length ? isVisible : !opened;
        if (!resolvedVisible && !this._shouldClearFilter()) {
            this._restoreInputText(true)
        }
        if (this._wasSearch() && resolvedVisible) {
            this._wasSearch(false);
            const showDataImmediately = showDataBeforeSearch || this._isMinSearchLengthExceeded();
            if (showDataImmediately && this._dataController.getDataSource()) {
                if (this._searchTimer) {
                    return
                }
                const searchValue = this._getActualSearchValue();
                if (searchValue) {
                    this._wasSearch(true)
                }
                this._filterDataSource(searchValue || null)
            } else {
                this._setListOption("items", [])
            }
        }
        if (resolvedVisible) {
            this._scrollToSelectedItem()
        }
        super._toggleOpenState(resolvedVisible)
    }
    _renderTooltip() {
        const {
            tooltipEnabled: tooltipEnabled,
            displayValue: displayValue
        } = this.option();
        if (tooltipEnabled) {
            this.$element().attr("title", displayValue ?? "")
        }
    }
    _renderDimensions() {
        super._renderDimensions();
        this._updatePopupWidth();
        this._updateListDimensions()
    }
    _isValueEqualInputText() {
        const {
            selectedItem: initialSelectedItem
        } = this.option();
        if (null === initialSelectedItem) {
            return false
        }
        const value = this._displayGetter(initialSelectedItem);
        const displayValue = value ? String(value) : "";
        const inputText = this._searchValue();
        return displayValue === inputText
    }
    _popupHidingHandler() {
        if (this._isValueEqualInputText()) {
            this._cancelEditing()
        }
        super._popupHidingHandler()
    }
    _popupHiddenHandler() {
        super._popupHiddenHandler();
        if (this._shouldCancelSearch()) {
            this._wasSearch(false);
            this._searchCanceled();
            this._shouldCancelSearch(false)
        }
    }
    _restoreInputText(saveEditingValue) {
        var _this$_loadItemDeferr;
        const {
            readOnly: readOnly,
            searchEnabled: searchEnabled,
            allowClearing: allowClearing
        } = this.option();
        if (readOnly) {
            return
        }
        null === (_this$_loadItemDeferr = this._loadItemDeferred) || void 0 === _this$_loadItemDeferr || _this$_loadItemDeferr.always(() => {
            const {
                acceptCustomValue: acceptCustomValue,
                text: text,
                selectedItem: initialSelectedItem,
                customItemCreateEvent: customItemCreateEvent
            } = this.option();
            if (acceptCustomValue) {
                if (!saveEditingValue && !this._isValueChanging) {
                    let initialItem = null;
                    if ((0, _type.isDefined)(initialSelectedItem)) {
                        initialItem = initialSelectedItem
                    } else if ("" !== customItemCreateEvent) {
                        initialItem = this._createCustomItem(text ?? "")
                    }
                    this._updateField(initialItem);
                    this._clearFilter()
                }
                return
            }
            if (searchEnabled) {
                if (!this._searchValue() && allowClearing) {
                    this._clearTextValue();
                    return
                }
            }
            if (this._isValueEqualInputText()) {
                return
            }
            this._renderInputValue().always(selectedItem => {
                const newSelectedItem = (0, _common.ensureDefined)(selectedItem, initialSelectedItem);
                this._setSelectedItem(newSelectedItem);
                this._updateField(newSelectedItem);
                this._clearFilter()
            })
        })
    }
    _valueChangeEventIncludesBlur() {
        const {
            [this._getValueChangeEventOptionName()]: valueChangeEvent
        } = this.option();
        return valueChangeEvent.includes("blur")
    }
    _isPreventedFocusOutEvent(e) {
        return this._preventNestedFocusEvent(e) || this._valueChangeEventIncludesBlur()
    }
    _focusOutHandler(e) {
        if (!this._isPreventedFocusOutEvent(e)) {
            const isOverlayTarget = this._isOverlayNestedTarget(e.relatedTarget);
            if (!isOverlayTarget) {
                this._restoreInputText();
                this._clearSearchTimer()
            }
            this._cancelSearchIfNeed(e)
        }
        e.target = this._input().get(0);
        super._focusOutHandler(e)
    }
    _cancelSearchIfNeed(e) {
        const {
            searchEnabled: searchEnabled
        } = this.option();
        const isOverlayTarget = this._isOverlayNestedTarget(null === e || void 0 === e ? void 0 : e.relatedTarget);
        const shouldCancelSearch = this._wasSearch() && searchEnabled && !isOverlayTarget;
        if (shouldCancelSearch) {
            var _this$_popup5;
            const isPopupVisible = null === (_this$_popup5 = this._popup) || void 0 === _this$_popup5 ? void 0 : _this$_popup5._hideAnimationProcessing;
            this._clearSearchTimer();
            if (isPopupVisible) {
                this._shouldCancelSearch(true)
            } else {
                this._wasSearch(false);
                this._searchCanceled()
            }
        }
    }
    _shouldCancelSearch(value) {
        if (void 0 === value) {
            return this._shouldCancelSearchValue ?? false
        }
        this._shouldCancelSearchValue = value;
        return value
    }
    _isOverlayNestedTarget(target) {
        return !!(0, _renderer.default)(target).closest(".dx-selectbox-popup-wrapper").length
    }
    _clearTextValue() {
        const {
            selectedItem: selectedItem
        } = this.option();
        const selectedItemText = this._displayGetter(selectedItem);
        const shouldRestoreValue = selectedItem && "" !== selectedItemText;
        if (shouldRestoreValue) {
            if (this._savedTextRemoveEvent) {
                this._saveValueChangeEvent(this._savedTextRemoveEvent)
            }
            this.option("value", null)
        }
        delete this._savedTextRemoveEvent
    }
    _shouldOpenPopup() {
        return this._needPassDataSourceToList() && Boolean(this._wasSearch())
    }
    _isFocused() {
        const activeElement = _dom_adapter.default.getActiveElement(this.element());
        return super._isFocused() && (0, _renderer.default)(activeElement).closest(this._input()).length > 0
    }
    _getValueChangeEventOptionName() {
        return "customItemCreateEvent"
    }
    _renderValueChangeEvent() {
        if (this._isEditable()) {
            super._renderValueChangeEvent()
        }
    }
    _fieldRenderData() {
        const {
            focusedElement: focusedElement,
            opened: opened,
            selectedItem: selectedItem
        } = this.option();
        const $listFocused = this._list && opened ? (0, _renderer.default)(focusedElement) : null;
        if (null !== $listFocused && void 0 !== $listFocused && $listFocused.length) {
            return this._list._getItemData($listFocused)
        }
        return selectedItem
    }
    _isSelectedValue(value, cache) {
        const {
            value: currentValue
        } = this.option();
        return this._isValueEquals(value, currentValue)
    }
    _shouldCloseOnItemClick() {
        const {
            selectionMode: selectionMode,
            showSelectionControls: showSelectionControls
        } = this.option();
        return !(showSelectionControls && "single" !== selectionMode)
    }
    _listItemClickHandler(e) {
        if (!e) {
            return
        }
        const {
            searchEnabled: searchEnabled
        } = this.option();
        const previousValue = this._getCurrentValue();
        this._focusListElement((0, _renderer.default)(e.itemElement));
        this._saveValueChangeEvent(e.event);
        this._completeSelection(this._valueGetter(e.itemData));
        if (this._shouldCloseOnItemClick()) {
            this.option("opened", false)
        }
        if (searchEnabled && previousValue === this._valueGetter(e.itemData)) {
            this._updateField(e.itemData)
        }
        if (this._shouldClearFilter()) {
            this._cancelSearchIfNeed()
        }
    }
    _shouldClearFilter() {
        return this._wasSearch()
    }
    _completeSelection(value) {
        this._setValue(value)
    }
    _loadItem(value, cache) {
        const deferred = (0, _deferred.Deferred)();
        super._loadItem(value, cache).done(item => {
            deferred.resolve(item)
        }).fail(args => {
            if (null !== args && void 0 !== args && args.shouldSkipCallback) {
                return
            }
            const {
                selectedItem: selectedItem,
                acceptCustomValue: acceptCustomValue
            } = this.option();
            if (acceptCustomValue && value === this._valueGetter(selectedItem)) {
                deferred.resolve(selectedItem)
            } else {
                deferred.reject()
            }
        });
        return deferred.promise()
    }
    _loadInputValue(value, callback) {
        this._loadItemDeferred = this._loadItem(value).always(callback);
        return this._loadItemDeferred
    }
    _isCustomItemSelected() {
        const {
            selectedItem: selectedItem
        } = this.option();
        const searchValue = this._searchValue();
        const selectedItemText = this._displayGetter(selectedItem);
        return searchValue !== (null === selectedItemText || void 0 === selectedItemText ? void 0 : selectedItemText.toString())
    }
    _valueChangeEventHandler(e) {
        const {
            acceptCustomValue: acceptCustomValue
        } = this.option();
        if (acceptCustomValue && this._isCustomItemSelected() && !this._isValueChanging) {
            this._isValueChanging = true;
            this._customItemAddedHandler(e)
        }
    }
    _initCustomItemCreatingAction() {
        this._customItemCreatingAction = this._createActionByOption("onCustomItemCreating")
    }
    _createCustomItem(text) {
        const params = {
            text: text
        };
        const actionResult = this._customItemCreatingAction(params);
        const item = (0, _common.ensureDefined)(actionResult, params.customItem);
        if ((0, _type.isDefined)(actionResult)) {
            _errors.default.log("W0015", "onCustomItemCreating", "customItem")
        }
        return item
    }
    _customItemAddedHandler(e) {
        const searchValue = this._searchValue();
        const item = this._createCustomItem(searchValue);
        this._saveValueChangeEvent(e);
        if (void 0 === item) {
            this._renderValue();
            throw _errors.default.Error("E0121")
        }
        if ((0, _type.isPromise)(item)) {
            (0, _deferred.fromPromise)(item).done(this._setCustomItem.bind(this)).fail(this._setCustomItem.bind(this, null))
        } else {
            this._setCustomItem(item)
        }
    }
    _setCustomItem(item) {
        if (this._disposed) {
            return
        }
        const normalizedItem = item ?? null;
        this.option("selectedItem", normalizedItem);
        this._cancelSearchIfNeed();
        this._setValue(this._valueGetter(normalizedItem));
        this._renderDisplayText(this._displayGetter(normalizedItem));
        this._isValueChanging = false
    }
    _clearValueHandler(e) {
        this._preventFiltering = true;
        super._clearValueHandler(e);
        this._searchCanceled();
        return false
    }
    _wasSearch(value) {
        if (!arguments.length) {
            return !!this._wasSearchValue
        }
        this._wasSearchValue = value;
        return
    }
    _searchHandler(e) {
        if (this._preventFiltering) {
            delete this._preventFiltering;
            return
        }
        if (this._needPassDataSourceToList()) {
            this._wasSearch(true)
        }
        super._searchHandler(e)
    }
    _dataSourceFiltered(searchValue) {
        super._dataSourceFiltered();
        if (null !== searchValue) {
            this._renderInputSubstitution();
            this._renderFocusedElement()
        }
    }
    _valueSubstituted() {
        const input = this._input().get(0);
        const currentSearchLength = this._searchValue().length;
        const isAllSelected = 0 === input.selectionStart && input.selectionEnd === currentSearchLength;
        const inputHasSelection = input.selectionStart !== input.selectionEnd;
        const isLastSymbolSelected = currentSearchLength === input.selectionEnd;
        return Boolean(this._wasSearch() && inputHasSelection && !isAllSelected && isLastSymbolSelected && this._shouldSubstitutionBeRendered())
    }
    _shouldSubstitutionBeRendered() {
        return !this._preventSubstitution && Boolean(this._isInlineAutocompleteEnabled())
    }
    _renderInputSubstitution() {
        var _this$_list5;
        if (!this._shouldSubstitutionBeRendered()) {
            delete this._preventSubstitution;
            return
        }
        const {
            items: items
        } = (null === (_this$_list5 = this._list) || void 0 === _this$_list5 ? void 0 : _this$_list5.option()) ?? {};
        const item = this._list && this._getPlainItems(items)[0];
        if (!item) {
            return
        }
        const $input = this._input();
        const valueLength = $input.val().length;
        if (0 === valueLength) {
            return
        }
        const inputElement = $input.get(0);
        const displayValue = this._displayGetter(item).toString();
        inputElement.value = displayValue;
        this._caret({
            start: valueLength,
            end: displayValue.length
        })
    }
    _shouldLogFieldTemplateDeprecationWarning() {
        return true
    }
    _dispose() {
        this._renderInputValueAsync = _common.noop;
        delete this._loadItemDeferred;
        super._dispose()
    }
    _optionChanged(args) {
        switch (args.name) {
            case "customItemCreateEvent":
                this._refreshValueChangeEvent();
                this._refreshFocusEvent();
                this._refreshEvents();
                break;
            case "onCustomItemCreating":
                this._initCustomItemCreatingAction();
                break;
            case "tooltipEnabled":
                this._renderTooltip();
                break;
            case "readOnly":
            case "disabled":
            case "searchMode":
                super._optionChanged(args);
                this._setDefaultAria();
                break;
            case "displayCustomValue":
            case "acceptCustomValue":
            case "showSelectionControls":
                this._invalidate();
                break;
            case "allowClearing":
                break;
            default:
                super._optionChanged(args)
        }
    }
}(0, _component_registrator.default)("dxSelectBox", SelectBox);
var _default = exports.default = SelectBox;
