/**
 * DevExtreme (esm/__internal/core/license/lcp_key_validation/license_info.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    isProduct
} from "./product_info";
import {
    ProductKind
} from "./types";
export function isLicenseValid(info) {
    return Array.isArray(info.products) && info.products.length > 0
}
export function getMaxExpiration(info) {
    const expirationDates = info.products.map(p => p.expiration).filter(e => e > 0 && e !== 1 / 0);
    if (0 === expirationDates.length) {
        return 1 / 0
    }
    return Math.max(...expirationDates)
}
export function findLatestDevExtremeVersion(info) {
    var _sorted$find;
    let acceptAspNetEntitlement = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : false;
    if (!isLicenseValid(info)) {
        return
    }
    const productKinds = acceptAspNetEntitlement ? [ProductKind.DevExtremeHtmlJs, ProductKind.DevExtremeAspNet] : [ProductKind.DevExtremeHtmlJs];
    const sorted = [...info.products].sort((a, b) => b.version - a.version);
    return null === (_sorted$find = sorted.find(p => isProduct(p, ...productKinds))) || void 0 === _sorted$find ? void 0 : _sorted$find.version
}
