/**
 * DevExtreme (esm/__internal/scheduler/workspaces/view_model/view_data_generator_week.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    weekUtils
} from "../../r1/utils/index";
import {
    ViewDataGenerator
} from "./view_data_generator";
export class ViewDataGeneratorWeek extends ViewDataGenerator {
    _getIntervalDuration(intervalCount) {
        return weekUtils.getIntervalDuration(intervalCount)
    }
    calculateStartViewDate(options) {
        return weekUtils.calculateStartViewDate(options.currentDate, options.startDayHour, options.startDate, this._getIntervalDuration(options.intervalCount), this.getFirstDayOfWeek(options.firstDayOfWeek), options.skippedDays ?? this.skippedDays)
    }
    getSkippedDaysAnchorDay(firstDayOfWeekOption, startViewDate) {
        return this.skippedDays.length > 0 ? startViewDate.getDay() : this.getFirstDayOfWeek(firstDayOfWeekOption ?? 0)
    }
}
