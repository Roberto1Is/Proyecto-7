/**
 * DevExtreme (esm/__internal/ui/drop_down_button.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import registerComponent from "../../core/component_registrator";
import $ from "../../core/renderer";
import {
    compileGetter
} from "../../core/utils/data";
import {
    Deferred
} from "../../core/utils/deferred";
import {
    extend
} from "../../core/utils/extend";
import {
    getImageContainer
} from "../../core/utils/icon";
import ButtonGroup from "../../ui/button_group";
import messageLocalization from "../core/localization/message";
import {
    getPublicElement
} from "../core/m_element";
import {
    Guid
} from "../core/m_guid";
import {
    FunctionTemplate
} from "../core/templates/m_function_template";
import {
    ensureDefined
} from "../core/utils/m_common";
import {
    isDefined,
    isObject,
    isPlainObject
} from "../core/utils/m_type";
import Widget from "../core/widget/widget";
import DataController from "../data/data_controller/data_controller";
import {
    getElementWidth
} from "../ui/drop_down_editor/utils";
import List from "../ui/list/list.edit.search";
import Popup from "../ui/popup/popup";
const DROP_DOWN_BUTTON_CLASS = "dx-dropdownbutton";
const DROP_DOWN_BUTTON_CONTENT = "dx-dropdownbutton-content";
const DROP_DOWN_BUTTON_ACTION_CLASS = "dx-dropdownbutton-action";
const DROP_DOWN_BUTTON_TOGGLE_CLASS = "dx-dropdownbutton-toggle";
const DROP_DOWN_BUTTON_HAS_ARROW_CLASS = "dx-dropdownbutton-has-arrow";
const DROP_DOWN_BUTTON_POPUP_WRAPPER_CLASS = "dx-dropdownbutton-popup-wrapper";
const DROP_DOWN_EDITOR_OVERLAY_CLASS = "dx-dropdowneditor-overlay";
const DX_BUTTON_TEXT_CLASS = "dx-button-text";
const DX_BUTTON_CLASS = "dx-button";
const DX_ICON_RIGHT_CLASS = "dx-icon-right";
const OVERLAY_CONTENT_LABEL = "Dropdown";
class DropDownButton extends Widget {
    _getDefaultOptions() {
        return Object.assign({}, super._getDefaultOptions(), {
            itemTemplate: "item",
            keyExpr: "this",
            selectedItem: null,
            selectedItemKey: null,
            stylingMode: "outlined",
            deferRendering: true,
            noDataText: messageLocalization.format("dxCollectionWidget-noDataText"),
            useSelectMode: false,
            splitButton: false,
            showArrowIcon: true,
            template: null,
            text: "",
            type: "normal",
            onButtonClick: null,
            onSelectionChanged: null,
            onItemClick: null,
            opened: false,
            items: null,
            dataSource: null,
            focusStateEnabled: true,
            hoverStateEnabled: true,
            dropDownOptions: {},
            dropDownContentTemplate: "content",
            wrapItemText: false,
            useItemTextAsTitle: true,
            grouped: false,
            groupTemplate: "group",
            buttonGroupOptions: {},
            _cached_buttonGroupOptions: {},
            _cached_dropDownOptions: {}
        })
    }
    _setOptionsByReference() {
        super._setOptionsByReference();
        extend(this._optionsByReference, {
            selectedItem: true
        })
    }
    _init() {
        super._init();
        this._createItemClickAction();
        this._createActionClickAction();
        this._createSelectionChangedAction();
        this._initDataController();
        this._compileKeyGetter();
        this._compileDisplayGetter();
        const {
            buttonGroupOptions: buttonGroupOptions,
            dropDownOptions: dropDownOptions
        } = this.option();
        this._options.cache("buttonGroupOptions", buttonGroupOptions);
        this._options.cache("dropDownOptions", dropDownOptions)
    }
    _initDataController() {
        const {
            dataSource: dataSource,
            items: items,
            keyExpr: keyExpr
        } = this.option();
        this._dataController = new DataController(dataSource ?? items, {
            key: keyExpr
        })
    }
    _initTemplates() {
        this._templateManager.addDefaultTemplates({
            content: new FunctionTemplate(options => {
                const $popupContent = $(options.container);
                const $listContainer = $("<div>").appendTo($popupContent);
                this._list = this._createComponent($listContainer, List, this._listOptions());
                this._list.registerKeyHandler("escape", this._escHandler.bind(this));
                this._list.registerKeyHandler("tab", this._escHandler.bind(this));
                this._list.registerKeyHandler("leftArrow", this._escHandler.bind(this));
                this._list.registerKeyHandler("rightArrow", this._escHandler.bind(this))
            })
        });
        super._initTemplates()
    }
    _compileKeyGetter() {
        const key = this._dataController.key();
        if (void 0 !== key) {
            this._keyGetter = compileGetter(key)
        }
    }
    _compileDisplayGetter() {
        const {
            displayExpr: displayExpr
        } = this.option();
        this._displayGetter = compileGetter(displayExpr)
    }
    _initMarkup() {
        super._initMarkup();
        this.$element().addClass("dx-dropdownbutton");
        this._renderButtonGroup();
        this._updateArrowClass();
        if (isDefined(this.option("selectedItemKey"))) {
            this._loadSelectedItem().done(this._updateActionButton.bind(this))
        }
    }
    _renderFocusTarget() {}
    _render() {
        if (!this.option("deferRendering") || this.option("opened")) {
            this._renderPopup()
        }
        super._render()
    }
    _renderContentImpl() {
        if (this._popup) {
            this._renderPopupContent()
        }
        return super._renderContentImpl()
    }
    _loadSelectedItem() {
        var _this$_loadSingleDefe;
        null === (_this$_loadSingleDefe = this._loadSingleDeferred) || void 0 === _this$_loadSingleDefe || _this$_loadSingleDefe.reject();
        const d = Deferred();
        if (this._list && void 0 !== this._lastSelectedItemData) {
            const cachedResult = this.option("useSelectMode") ? this._list.option("selectedItem") : this._lastSelectedItemData;
            return d.resolve(cachedResult)
        }
        this._lastSelectedItemData = void 0;
        const {
            selectedItemKey: selectedItemKey
        } = this.option();
        if (isDefined(selectedItemKey)) {
            this._dataController.loadSingle(this._dataController.key(), selectedItemKey).done(item => {
                d.resolve(item)
            }).fail(() => {
                d.reject(null)
            })
        } else {
            d.reject(null)
        }
        this._loadSingleDeferred = d;
        return d.promise()
    }
    _createActionClickAction() {
        this._actionClickAction = this._createActionByOption("onButtonClick")
    }
    _createSelectionChangedAction() {
        this._selectionChangedAction = this._createActionByOption("onSelectionChanged")
    }
    _createItemClickAction() {
        this._itemClickAction = this._createActionByOption("onItemClick")
    }
    _fireSelectionChangedAction(_ref) {
        let {
            previousValue: previousValue,
            value: value
        } = _ref;
        this._selectionChangedAction({
            item: value,
            previousItem: previousValue
        })
    }
    _fireItemClickAction(_ref2) {
        let {
            event: event,
            itemElement: itemElement,
            itemData: itemData
        } = _ref2;
        return this._itemClickAction({
            event: event,
            itemElement: itemElement,
            itemData: this._actionItem ?? itemData
        })
    }
    _formButtonTemplateFn(_ref3, buttonContent) {
        let {
            text: text,
            icon: icon
        } = _ref3;
        const $firstIcon = getImageContainer(icon);
        const $textContainer = text ? $("<span>").text(text).addClass("dx-button-text") : void 0;
        const $secondIcon = getImageContainer("spindown");
        if (null !== $secondIcon) {
            $secondIcon.addClass("dx-icon-right")
        }
        const $container = $(buttonContent);
        if ($firstIcon) {
            $container.append($firstIcon)
        }
        if ($textContainer) {
            $container.append($textContainer)
        }
        if ($secondIcon) {
            $container.append($secondIcon)
        }
    }
    _getButtonTemplate() {
        const {
            template: template,
            splitButton: splitButton,
            showArrowIcon: showArrowIcon
        } = this.option();
        if (template) {
            return template
        }
        return splitButton || !showArrowIcon ? "content" : this._formButtonTemplateFn.bind(this)
    }
    _getActionButtonConfig() {
        const {
            icon: icon,
            text: text,
            type: type,
            splitButton: splitButton
        } = this.option();
        const actionButtonConfig = {
            text: text,
            icon: icon,
            type: type,
            template: this._getButtonTemplate(),
            elementAttr: {
                class: "dx-dropdownbutton-action",
                role: splitButton ? "menuitem" : void 0
            }
        };
        return actionButtonConfig
    }
    _getSpinButtonConfig() {
        const {
            type: type
        } = this.option();
        const config = {
            type: type,
            icon: "spindown",
            elementAttr: {
                class: "dx-dropdownbutton-toggle",
                role: "menuitem"
            }
        };
        return config
    }
    _getButtonGroupItems() {
        const {
            splitButton: splitButton
        } = this.option();
        const items = [this._getActionButtonConfig()];
        if (splitButton) {
            items.push(this._getSpinButtonConfig())
        }
        return items
    }
    _buttonGroupItemClick(_ref4) {
        var _itemData$elementAttr;
        let {
            event: event,
            itemData: itemData
        } = _ref4;
        const itemClass = null === itemData || void 0 === itemData || null === (_itemData$elementAttr = itemData.elementAttr) || void 0 === _itemData$elementAttr ? void 0 : _itemData$elementAttr.class;
        const isActionButton = "dx-dropdownbutton-action" === itemClass;
        const isToggleButton = "dx-dropdownbutton-toggle" === itemClass;
        if (isToggleButton) {
            this.toggle()
        } else if (isActionButton) {
            this._actionClickAction({
                event: event,
                selectedItem: this.option("selectedItem")
            });
            if (!this.option("splitButton")) {
                this.toggle()
            }
        }
    }
    _getButtonGroupOptions() {
        const {
            accessKey: accessKey,
            focusStateEnabled: focusStateEnabled,
            hoverStateEnabled: hoverStateEnabled,
            splitButton: splitButton,
            stylingMode: stylingMode,
            tabIndex: tabIndex
        } = this.option();
        const buttonGroupOptions = Object.assign({
            items: this._getButtonGroupItems(),
            width: "100%",
            height: "100%",
            selectionMode: "none",
            focusStateEnabled: focusStateEnabled,
            hoverStateEnabled: hoverStateEnabled,
            stylingMode: stylingMode,
            accessKey: accessKey,
            tabIndex: tabIndex,
            elementAttr: {
                role: splitButton ? "menu" : "group"
            },
            onItemClick: this._buttonGroupItemClick.bind(this),
            onKeyboardHandled: e => this._keyboardHandler(e)
        }, this._options.cache("buttonGroupOptions"));
        return buttonGroupOptions
    }
    _renderPopupContent() {
        var _this$_popup;
        const $content = null === (_this$_popup = this._popup) || void 0 === _this$_popup ? void 0 : _this$_popup.$content();
        if (!$content) {
            return
        }
        const template = this._getTemplateByOption("dropDownContentTemplate");
        $content.empty();
        this._popupContentId = `dx-${new Guid}`;
        this.setAria("id", this._popupContentId, $content);
        return template.render({
            container: getPublicElement($content),
            model: this.option("items") || this._dataController.getDataSource()
        })
    }
    _popupOptions() {
        const horizontalAlignment = this.option("rtlEnabled") ? "right" : "left";
        const {
            deferRendering: deferRendering,
            opened: opened
        } = this.option();
        const cachedDropDownOptions = this._options.cache("dropDownOptions");
        const position = {
            of: this.$element(),
            collision: "flipfit",
            my: `${horizontalAlignment} top`,
            at: `${horizontalAlignment} bottom`
        };
        return Object.assign({
            dragEnabled: false,
            focusStateEnabled: false,
            deferRendering: deferRendering,
            hideOnOutsideClick: e => {
                const $element = this.$element();
                const $buttonClicked = $(e.target).closest(".dx-dropdownbutton");
                return !$buttonClicked.is($element)
            },
            showTitle: false,
            animation: {
                show: {
                    type: "fade",
                    duration: 0,
                    from: 0,
                    to: 1
                },
                hide: {
                    type: "fade",
                    duration: 400,
                    from: 1,
                    to: 0
                }
            },
            width: getElementWidth(this.$element()),
            height: "auto",
            shading: false,
            position: position,
            _wrapperClassExternal: "dx-dropdowneditor-overlay",
            contentTemplate: null
        }, cachedDropDownOptions, {
            visible: opened
        })
    }
    _listOptions() {
        const {
            wrapItemText: wrapItemText,
            focusStateEnabled: focusStateEnabled,
            hoverStateEnabled: hoverStateEnabled,
            useItemTextAsTitle: useItemTextAsTitle,
            grouped: grouped,
            groupTemplate: groupTemplate,
            noDataText: noDataText,
            displayExpr: displayExpr,
            itemTemplate: itemTemplate,
            items: items,
            selectedItemKey: selectedItemKey,
            useSelectMode: useSelectMode
        } = this.option();
        return {
            selectionMode: useSelectMode ? "single" : "none",
            wrapItemText: wrapItemText,
            focusStateEnabled: focusStateEnabled,
            hoverStateEnabled: hoverStateEnabled,
            useItemTextAsTitle: useItemTextAsTitle,
            onContentReady: () => this._fireContentReadyAction(),
            selectedItemKeys: isDefined(selectedItemKey) && useSelectMode ? [selectedItemKey] : [],
            grouped: grouped,
            groupTemplate: groupTemplate,
            keyExpr: this._dataController.key(),
            noDataText: noDataText,
            displayExpr: displayExpr,
            itemTemplate: itemTemplate,
            items: items,
            dataSource: this._dataController.getDataSource(),
            onItemClick: e => {
                var _this$_keyGetter;
                if (!this.option("useSelectMode")) {
                    this._lastSelectedItemData = e.itemData
                }
                this.option("selectedItemKey", null === (_this$_keyGetter = this._keyGetter) || void 0 === _this$_keyGetter ? void 0 : _this$_keyGetter.call(this, e.itemData));
                const actionResult = this._fireItemClickAction(e);
                if (false !== actionResult) {
                    this.toggle(false);
                    this._buttonGroup.focus()
                }
            }
        }
    }
    _upDownKeyHandler() {
        var _this$_popup2;
        if (null !== (_this$_popup2 = this._popup) && void 0 !== _this$_popup2 && _this$_popup2.option("visible") && this._list) {
            this._list.focus()
        } else {
            this.open()
        }
        return true
    }
    _escHandler() {
        this.close();
        this._buttonGroup.focus();
        return true
    }
    _tabHandler() {
        this.close();
        return true
    }
    _renderPopup() {
        var _this$_popup$$content, _this$_popup$$wrapper;
        const $popup = $("<div>");
        this.$element().append($popup);
        this._popup = this._createComponent($popup, Popup, this._popupOptions());
        null === (_this$_popup$$content = this._popup.$content()) || void 0 === _this$_popup$$content || _this$_popup$$content.addClass(DROP_DOWN_BUTTON_CONTENT);
        null === (_this$_popup$$wrapper = this._popup.$wrapper()) || void 0 === _this$_popup$$wrapper || _this$_popup$$wrapper.addClass("dx-dropdownbutton-popup-wrapper");
        this._popup.$overlayContent().attr("aria-label", "Dropdown");
        this._popup.on("hiding", this._popupHidingHandler.bind(this));
        this._popup.on("showing", this._popupShowingHandler.bind(this));
        this._bindInnerWidgetOptions(this._popup, "dropDownOptions")
    }
    _popupHidingHandler() {
        this.option("opened", false);
        this._updateAriaAttributes(false)
    }
    _popupOptionChanged(args) {
        const options = Widget.getOptionsFromContainer(args);
        this._setPopupOption(options);
        const optionsKeys = Object.keys(options);
        if (optionsKeys.includes("width") || optionsKeys.includes("height")) {
            this._dimensionChanged()
        }
    }
    _dimensionChanged() {
        var _this$_options$cache;
        const cachedWidth = null === (_this$_options$cache = this._options.cache("dropDownOptions")) || void 0 === _this$_options$cache ? void 0 : _this$_options$cache.width;
        if (void 0 === cachedWidth) {
            this._setPopupOption("width", getElementWidth(this.$element()))
        }
    }
    _setPopupOption() {
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
            args[_key] = arguments[_key]
        }
        this._setWidgetOption("_popup", args)
    }
    _popupShowingHandler() {
        this.option("opened", true);
        this._updateAriaAttributes(true)
    }
    _setElementAria(value) {
        const elementAria = {
            owns: value ? this._popupContentId : void 0
        };
        this.setAria(elementAria, this.$element())
    }
    _setButtonsAria(value) {
        const commonButtonAria = {
            expanded: value,
            haspopup: "listbox"
        };
        const firstButtonAria = {};
        if (!this.option("text")) {
            firstButtonAria.label = "dropdownbutton"
        }
        this._getButtons().each((index, $button) => {
            if (0 === index) {
                this.setAria(Object.assign({}, firstButtonAria, commonButtonAria), $($button))
            } else {
                this.setAria(commonButtonAria, $($button))
            }
            return true
        })
    }
    _updateAriaAttributes(value) {
        this._setElementAria(value);
        this._setButtonsAria(value)
    }
    _getButtons() {
        return this._buttonGroup.$element().find(".dx-button")
    }
    _renderButtonGroup() {
        var _this$_buttonGroup;
        const $buttonGroup = (null === (_this$_buttonGroup = this._buttonGroup) || void 0 === _this$_buttonGroup ? void 0 : _this$_buttonGroup.$element()) || $("<div>");
        if (!this._buttonGroup) {
            this.$element().append($buttonGroup)
        }
        this._buttonGroup = this._createComponent($buttonGroup, ButtonGroup, this._getButtonGroupOptions());
        this._buttonGroup.registerKeyHandler("downArrow", this._upDownKeyHandler.bind(this));
        this._buttonGroup.registerKeyHandler("tab", this._tabHandler.bind(this));
        this._buttonGroup.registerKeyHandler("upArrow", this._upDownKeyHandler.bind(this));
        this._buttonGroup.registerKeyHandler("escape", this._escHandler.bind(this));
        this._bindInnerWidgetOptions(this._buttonGroup, "buttonGroupOptions");
        const {
            opened: opened
        } = this.option();
        this._updateAriaAttributes(opened ?? false)
    }
    _updateArrowClass() {
        const hasArrow = this.option("splitButton") || this.option("showArrowIcon");
        this.$element().toggleClass("dx-dropdownbutton-has-arrow", Boolean(hasArrow))
    }
    toggle(visible) {
        var _this$_popup3;
        if (!this._popup) {
            this._renderPopup();
            this._renderContent()
        }
        return null === (_this$_popup3 = this._popup) || void 0 === _this$_popup3 ? void 0 : _this$_popup3.toggle(visible)
    }
    open() {
        return this.toggle(true)
    }
    close() {
        return this.toggle(false)
    }
    _setListOption(name, value) {
        var _this$_list;
        null === (_this$_list = this._list) || void 0 === _this$_list || _this$_list.option(name, value)
    }
    _getDisplayValue(item) {
        var _this$_displayGetter;
        const isPrimitiveItem = !isObject(item);
        const displayValue = isPrimitiveItem ? item : null === (_this$_displayGetter = this._displayGetter) || void 0 === _this$_displayGetter ? void 0 : _this$_displayGetter.call(this, item);
        return !isObject(displayValue) ? String(ensureDefined(displayValue, "")) : ""
    }
    _updateActionButton(selectedItem) {
        var _this$_keyGetter2;
        if (this.option("useSelectMode")) {
            this.option({
                text: this._getDisplayValue(selectedItem),
                icon: isPlainObject(selectedItem) ? selectedItem.icon : void 0
            })
        }
        this._setOptionWithoutOptionChange("selectedItem", selectedItem);
        this._setOptionWithoutOptionChange("selectedItemKey", null === (_this$_keyGetter2 = this._keyGetter) || void 0 === _this$_keyGetter2 ? void 0 : _this$_keyGetter2.call(this, selectedItem))
    }
    _clean() {
        var _this$_list2, _this$_popup4;
        null === (_this$_list2 = this._list) || void 0 === _this$_list2 || _this$_list2.$element().remove();
        null === (_this$_popup4 = this._popup) || void 0 === _this$_popup4 || _this$_popup4.$element().remove()
    }
    _selectedItemKeyChanged(value) {
        this._setListOption("selectedItemKeys", this.option("useSelectMode") && isDefined(value) ? [value] : []);
        const {
            selectedItem: previousItem
        } = this.option();
        this._loadSelectedItem().always(selectedItem => {
            var _this$_displayGetter2, _this$_displayGetter3;
            this._updateActionButton(selectedItem);
            if ((null === (_this$_displayGetter2 = this._displayGetter) || void 0 === _this$_displayGetter2 ? void 0 : _this$_displayGetter2.call(this, previousItem)) !== (null === (_this$_displayGetter3 = this._displayGetter) || void 0 === _this$_displayGetter3 ? void 0 : _this$_displayGetter3.call(this, selectedItem))) {
                this._fireSelectionChangedAction({
                    previousValue: previousItem,
                    value: selectedItem
                })
            }
        })
    }
    _updateButtonGroup(name, value) {
        this._buttonGroup.option(name, value);
        const {
            opened: opened
        } = this.option();
        this._updateAriaAttributes(opened ?? false)
    }
    _actionButtonOptionChanged(_ref5) {
        let {
            name: name,
            value: value
        } = _ref5;
        const newConfig = {};
        newConfig[name] = value;
        this._updateButtonGroup("items[0]", extend({}, this._getActionButtonConfig(), newConfig));
        if (this._popup) {
            this._popup.repaint()
        }
    }
    _selectModeChanged(value) {
        if (value) {
            this._setListOption("selectionMode", "single");
            const selectedItemKey = this.option("selectedItemKey");
            this._setListOption("selectedItemKeys", isDefined(selectedItemKey) ? [selectedItemKey] : []);
            this._selectedItemKeyChanged(this.option("selectedItemKey"))
        } else {
            this._setListOption("selectionMode", "none");
            this.option({
                selectedItemKey: void 0,
                selectedItem: void 0
            });
            const {
                text: text
            } = this.option();
            this._actionButtonOptionChanged({
                name: "text",
                value: text
            })
        }
    }
    _updateItemCollection(optionName) {
        const {
            selectedItemKey: selectedItemKey,
            useSelectMode: useSelectMode
        } = this.option();
        this._setListOption("selectedItem", null);
        this._setWidgetOption("_list", [optionName]);
        if (isDefined(selectedItemKey)) {
            this._loadSelectedItem().done(selectedItem => {
                if (useSelectMode) {
                    this._setListOption("selectedItemKeys", [selectedItemKey]);
                    this._setListOption("selectedItem", selectedItem)
                }
            }).fail(() => {
                this._setListOption("selectedItemKeys", [])
            }).always(this._updateActionButton.bind(this))
        }
    }
    _updateDataController(items) {
        const {
            keyExpr: keyExpr
        } = this.option();
        this._dataController.updateDataSource(items, keyExpr);
        this._updateKeyExpr()
    }
    _updateKeyExpr() {
        this._compileKeyGetter();
        this._setListOption("keyExpr", this._dataController.key())
    }
    focus() {
        this._buttonGroup.focus()
    }
    _optionChanged(args) {
        var _this$_popup5, _this$_popup6;
        const {
            name: name,
            value: value
        } = args;
        switch (name) {
            case "useSelectMode":
                this._selectModeChanged(value);
                break;
            case "splitButton":
                this._updateArrowClass();
                this._renderButtonGroup();
                break;
            case "displayExpr":
                this._compileDisplayGetter();
                this._setListOption(name, value);
                this._updateActionButton(this.option("selectedItem"));
                break;
            case "keyExpr":
                this._updateDataController();
                break;
            case "buttonGroupOptions":
                this._innerWidgetOptionChanged(this._buttonGroup, args);
                break;
            case "dropDownOptions":
                if ("dropDownOptions.visible" === args.fullName) {
                    break
                }
                if (void 0 !== args.value.visible) {
                    delete args.value.visible
                }
                this._popupOptionChanged(args);
                this._innerWidgetOptionChanged(this._popup, args);
                break;
            case "opened":
                this.toggle(value);
                break;
            case "focusStateEnabled":
            case "hoverStateEnabled":
                this._setListOption(name, value);
                this._updateButtonGroup(name, value);
                super._optionChanged(args);
                break;
            case "items": {
                const {
                    items: items
                } = this.option();
                this._updateDataController(items);
                this._updateItemCollection(name);
                break
            }
            case "dataSource":
                this._dataController.updateDataSource(value);
                this._updateKeyExpr();
                this._updateItemCollection(name);
                break;
            case "icon":
            case "text":
                this._actionButtonOptionChanged(args);
                break;
            case "showArrowIcon":
                this._updateArrowClass();
                this._renderButtonGroup();
                null === (_this$_popup5 = this._popup) || void 0 === _this$_popup5 || _this$_popup5.repaint();
                break;
            case "width":
            case "height":
                super._optionChanged(args);
                this._dimensionChanged();
                null === (_this$_popup6 = this._popup) || void 0 === _this$_popup6 || _this$_popup6.repaint();
                break;
            case "stylingMode":
            case "tabIndex":
                this._updateButtonGroup(name, value);
                break;
            case "type":
                this._updateButtonGroup("items", this._getButtonGroupItems());
                break;
            case "itemTemplate":
            case "grouped":
            case "noDataText":
            case "groupTemplate":
            case "wrapItemText":
            case "useItemTextAsTitle":
                this._setListOption(name, value);
                break;
            case "dropDownContentTemplate":
                this._renderContent();
                break;
            case "selectedItemKey":
                this._selectedItemKeyChanged(value);
                break;
            case "selectedItem":
            case "_cached_buttonGroupOptions":
            case "_cached_dropDownOptions":
                break;
            case "onItemClick":
                this._createItemClickAction();
                break;
            case "onButtonClick":
                this._createActionClickAction();
                break;
            case "onSelectionChanged":
                this._createSelectionChangedAction();
                break;
            case "deferRendering": {
                const {
                    opened: opened
                } = this.option();
                this.toggle(opened);
                break
            }
            case "template":
                this._renderButtonGroup();
                break;
            default:
                super._optionChanged(args)
        }
    }
    getDataSource() {
        return this._dataController.getDataSource()
    }
}
registerComponent("dxDropDownButton", DropDownButton);
export default DropDownButton;
