/**
 * DevExtreme (cjs/__internal/ui/autocomplete.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = void 0;
var _component_registrator = _interopRequireDefault(require("../../core/component_registrator"));
var _renderer = _interopRequireDefault(require("../../core/renderer"));
var _deferred = require("../../core/utils/deferred");
var _m_type = require("../core/utils/m_type");
var _index = require("../events/utils/index");
var _drop_down_list = _interopRequireDefault(require("../ui/drop_down_editor/drop_down_list"));

function _interopRequireDefault(e) {
    return e && e.__esModule ? e : {
        default: e
    }
}
const AUTOCOMPLETE_CLASS = "dx-autocomplete";
const AUTOCOMPLETE_POPUP_WRAPPER_CLASS = "dx-autocomplete-popup-wrapper";
class Autocomplete extends _drop_down_list.default {
    _supportedKeys() {
        let item = null;
        if (this._list) {
            const {
                focusedElement: focusedElement
            } = this._list.option();
            item = focusedElement ? (0, _renderer.default)(focusedElement) : null
        }
        const parent = super._supportedKeys();
        return Object.assign({}, parent, {
            upArrow(e) {
                if (parent.upArrow.call(this, e) && !(0, _index.isCommandKeyPressed)(e)) {
                    e.preventDefault();
                    e.stopPropagation();
                    if (item && !this._calcNextItem(-1)) {
                        this._clearFocusedItem();
                        return false
                    }
                }
                return true
            },
            downArrow(e) {
                if (parent.downArrow.call(this, e) && !(0, _index.isCommandKeyPressed)(e)) {
                    e.preventDefault();
                    e.stopPropagation();
                    if (item && !this._calcNextItem(1)) {
                        this._clearFocusedItem();
                        return false
                    }
                }
                return true
            },
            enter(e) {
                if (!item) {
                    this.close()
                }
                const {
                    opened: opened
                } = this.option();
                if (opened) {
                    e.preventDefault()
                }
                return Boolean(opened)
            }
        })
    }
    _getDefaultOptions() {
        return Object.assign({}, super._getDefaultOptions(), {
            minSearchLength: 1,
            maxItemCount: 10,
            noDataText: "",
            showDropDownButton: false,
            searchEnabled: true
        })
    }
    _initMarkup() {
        super._initMarkup();
        this.$element().addClass("dx-autocomplete")
    }
    _getAriaAutocomplete() {
        const {
            disabled: disabled,
            readOnly: readOnly
        } = this.option();
        const isInputEditable = !(readOnly || disabled);
        return isInputEditable ? "list" : "none"
    }
    _displayGetterExpr() {
        const {
            valueExpr: valueExpr
        } = this.option();
        return valueExpr
    }
    _closeOutsideDropDownHandler(_ref) {
        let {
            target: target
        } = _ref;
        return !(0, _renderer.default)(target).closest(this.$element()).length
    }
    _renderDimensions() {
        super._renderDimensions();
        this._updatePopupWidth();
        this._updateListDimensions()
    }
    _popupWrapperClass() {
        return `${super._popupWrapperClass()} dx-autocomplete-popup-wrapper`
    }
    _listConfig() {
        return Object.assign({}, super._listConfig(), {
            pageLoadMode: "none",
            onSelectionChanged: e => {
                this._setSelectedItem(e.addedItems[0])
            }
        })
    }
    _listItemClickHandler(e) {
        this._saveValueChangeEvent(e.event);
        const value = this._displayGetter(e.itemData);
        this.option("value", value);
        this.close()
    }
    _setListDataSource() {
        if (!this._list) {
            return
        }
        this._list.option("selectedItems", []);
        super._setListDataSource()
    }
    _refreshSelected() {}
    _searchCanceled() {
        super._searchCanceled();
        this.close()
    }
    _loadItem(value, cache) {
        const selectedItem = this._getItemFromPlain(value, cache);
        return (0, _deferred.Deferred)().resolve(selectedItem).promise()
    }
    _dataSourceOptions() {
        const {
            maxItemCount: maxItemCount
        } = this.option();
        return {
            paginate: true,
            pageSize: maxItemCount
        }
    }
    _searchDataSource(searchValue) {
        const {
            maxItemCount: maxItemCount
        } = this.option();
        if ((0, _m_type.isDefined)(maxItemCount)) {
            this._dataSource.pageSize(maxItemCount)
        }
        super._searchDataSource(searchValue);
        this._clearFocusedItem()
    }
    _clearFocusedItem() {
        if (this._list) {
            this._list.option("focusedElement", null);
            this._list.option("selectedIndex", -1)
        }
    }
    _renderValueEventName() {
        return "input keyup"
    }
    _valueChangeEventHandler(e) {
        const value = this._input().val() || null;
        return super._valueChangeEventHandler(e, value)
    }
    _optionChanged(args) {
        switch (args.name) {
            case "readOnly":
            case "disabled":
                super._optionChanged(args);
                this._setDefaultAria();
                break;
            case "maxItemCount":
                this._searchDataSource(this._searchValue());
                break;
            case "valueExpr":
                this._compileDisplayGetter();
                this._setListOption("displayExpr", this._displayGetterExpr());
                super._optionChanged(args);
                break;
            default:
                super._optionChanged(args)
        }
    }
    clear() {
        super.clear();
        this.close()
    }
    reset() {
        let value = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : void 0;
        if (arguments.length) {
            super.reset(value)
        } else {
            super.reset()
        }
        this.close()
    }
}(0, _component_registrator.default)("dxAutocomplete", Autocomplete);
var _default = exports.default = Autocomplete;
