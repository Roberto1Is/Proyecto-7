/**
 * DevExtreme (cjs/__internal/grids/grid_core/ai_assistant/ai_assistant_controller.js)
 * Version: 26.1.4
 * Build date: Fri Jul 24 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.AIAssistantController = void 0;
var _message = _interopRequireDefault(require("../../../../common/core/localization/message"));
var _data = require("../../../../common/data");
var _guid = _interopRequireDefault(require("../../../../core/guid"));
var _inflector = require("../../../../core/utils/inflector");
var _type = require("../../../../core/utils/type");
var _m_deferred = require("../../../core/utils/m_deferred");
var _m_modules = require("../m_modules");
var _ai_assistant_integration_controller = require("./ai_assistant_integration_controller");
var _index = require("./commands/index");
var _const = require("./const");
var _grid_commands = require("./grid_commands");
var _utils = require("./utils");

function _interopRequireDefault(e) {
    return e && e.__esModule ? e : {
        default: e
    }
}
class AIAssistantController extends _m_modules.Controller {
    constructor() {
        super(...arguments);
        this.processing = false
    }
    getCustomizedResponseTitle(status, commandNames) {
        const customizeResponseTitle = this.option("aiAssistant.customizeResponseTitle");
        if (!commandNames.length) {
            return _message.default.format("dxDataGrid-aiAssistantErrorMessage")
        }
        if (customizeResponseTitle && (0, _type.isFunction)(customizeResponseTitle)) {
            return customizeResponseTitle(status, commandNames)
        }
        if (1 === commandNames.length) {
            return (0, _inflector.captionize)(commandNames[0])
        }
        return [commandNames.slice(0, -1).map(_inflector.captionize).join(", "), (0, _inflector.captionize)(commandNames.at(-1))].join(" and ")
    }
    getCommandNames(actions) {
        const commandNames = actions.map(_ref => {
            let {
                name: name
            } = _ref;
            return name
        });
        const uniqueCommandNameSet = new Set(commandNames);
        return Array.from(uniqueCommandNameSet)
    }
    updateAIMessage(messageId, data) {
        var _this$messageStore;
        null === (_this$messageStore = this.messageStore) || void 0 === _this$messageStore || _this$messageStore.push([{
            type: "update",
            key: messageId,
            data: data
        }])
    }
    processResponse(response) {
        var _this$gridCommands, _this$gridCommands2, _this$gridCommands3;
        if (null !== (_this$gridCommands = this.gridCommands) && void 0 !== _this$gridCommands && _this$gridCommands.isExecuting()) {
            const localizedErrorMsg = _message.default.format("dxDataGrid-aiAssistantExecutionInProgressMessage");
            return Promise.reject(new Error(localizedErrorMsg))
        }
        if (!(null !== response && void 0 !== response && response.actions) || !Array.isArray(response.actions) || !response.actions.length) {
            const localizedErrorMsg = _message.default.format("dxDataGrid-aiAssistantInvalidResponseMessage");
            return Promise.reject(new Error(localizedErrorMsg))
        }
        const parsedActions = null === (_this$gridCommands2 = this.gridCommands) || void 0 === _this$gridCommands2 ? void 0 : _this$gridCommands2.parse(response.actions);
        if (!parsedActions) {
            const localizedErrorMsg = _message.default.format("dxDataGrid-aiAssistantInvalidResponseMessage");
            return Promise.reject(new Error(localizedErrorMsg))
        }
        const customizeResponseText = this.option("aiAssistant.customizeResponseText");
        const localizedErrorMsg = _message.default.format("dxDataGrid-aiAssistantUnexpectedErrorMessage");
        return (null === (_this$gridCommands3 = this.gridCommands) || void 0 === _this$gridCommands3 ? void 0 : _this$gridCommands3.executeCommands(parsedActions, customizeResponseText)) ?? Promise.reject(new Error(localizedErrorMsg))
    }
    createPendingAIMessage(message) {
        var _this$messageStore2;
        const parsedTimestamp = (0, _type.isString)(message.timestamp) ? Date.parse(message.timestamp) : message.timestamp ?? (new Date).getTime();
        const aiMessageId = `${_const.AI_ASSISTANT_AUTHOR_ID}-${String(new _guid.default)}`;
        const aiMessage = {
            id: aiMessageId,
            timestamp: parsedTimestamp,
            author: _const.AI_ASSISTANT_AUTHOR,
            prompt: message.text,
            headerText: _message.default.format("dxDataGrid-aiAssistantProcessingMessageHeader"),
            status: _const.MessageStatus.Pending,
            text: _const.MessageStatus.Pending
        };
        null === (_this$messageStore2 = this.messageStore) || void 0 === _this$messageStore2 || _this$messageStore2.push([{
            type: "insert",
            data: aiMessage
        }]);
        return aiMessage
    }
    completeAIMessage(messageId, commands, commandNames) {
        const messageStatus = (0, _utils.getMessageStatus)(commands);
        this.updateAIMessage(messageId, {
            headerText: this.getCustomizedResponseTitle(messageStatus, commandNames),
            commands: commands,
            status: messageStatus,
            text: messageStatus
        })
    }
    failAIMessage(messageId, error) {
        this.updateAIMessage(messageId, {
            headerText: _message.default.format("dxDataGrid-aiAssistantErrorMessageHeader"),
            errorText: error.message,
            status: _const.MessageStatus.Failure,
            text: _const.MessageStatus.Failure
        })
    }
    setProcessing(value) {
        this.processing = value
    }
    setAIMessageStatusToPending(aiMessage) {
        this.updateAIMessage(aiMessage.id, {
            headerText: _message.default.format("dxDataGrid-aiAssistantProcessingMessageHeader"),
            errorText: void 0,
            commands: void 0,
            status: _const.MessageStatus.Pending,
            text: _const.MessageStatus.Pending
        })
    }
    withProcessing(promise) {
        this.setProcessing(true);
        return promise.finally(() => {
            this.setProcessing(false)
        })
    }
    sendRequestToAICore(aiMessage) {
        return this.withProcessing(new Promise((resolve, reject) => {
            var _this$gridCommands4, _this$aiAssistantInte;
            const responseSchema = null === (_this$gridCommands4 = this.gridCommands) || void 0 === _this$gridCommands4 ? void 0 : _this$gridCommands4.buildResponseSchema();
            if (!responseSchema) {
                const localizedErrorMsg = _message.default.format("dxDataGrid-aiAssistantUnexpectedErrorMessage");
                const error = new Error(localizedErrorMsg);
                this.failAIMessage(aiMessage.id, error);
                reject(error);
                return
            }
            null === (_this$aiAssistantInte = this.aiAssistantIntegrationController) || void 0 === _this$aiAssistantInte || _this$aiAssistantInte.sendRequest(aiMessage.prompt, responseSchema, {
                onComplete: response => {
                    (0, _m_deferred.fromPromise)(this.processResponse(response)).done(commands => {
                        const commandNames = this.getCommandNames(response.actions);
                        this.completeAIMessage(aiMessage.id, commands, commandNames);
                        resolve()
                    }).fail(errorMessage => {
                        const error = errorMessage instanceof Error ? errorMessage : new Error(String(errorMessage));
                        this.failAIMessage(aiMessage.id, error);
                        reject(error)
                    })
                },
                onError: error => {
                    const localizedErrorMsg = _message.default.format("dxDataGrid-aiAssistantInvalidResponseMessage");
                    this.failAIMessage(aiMessage.id, new Error(localizedErrorMsg));
                    reject(error)
                },
                onAbort: () => {
                    const error = new Error(_message.default.format("dxDataGrid-aiAssistantAbortMessage"));
                    this.failAIMessage(aiMessage.id, error);
                    reject(error)
                }
            })
        }))
    }
    getGridCommandList() {
        return [..._index.coreCommands]
    }
    getAiAssistantIntegrationController() {
        return new _ai_assistant_integration_controller.AIAssistantIntegrationController(this.component)
    }
    init() {
        this.gridCommands = new _grid_commands.GridCommands(this.component, this.getGridCommandList());
        this.messageStore = new _data.ArrayStore({
            key: "id"
        });
        this.aiAssistantIntegrationController = this.getAiAssistantIntegrationController();
        this.aiAssistantIntegrationController.init()
    }
    getMessageStore() {
        return this.messageStore ?? new _data.ArrayStore({
            key: "id"
        })
    }
    sendRequestToAI(message) {
        if (this.processing) {
            const localizedErrorMsg = _message.default.format("dxDataGrid-aiAssistantRequestInProgressMessage");
            return Promise.reject(new Error(localizedErrorMsg))
        }
        if ((0, _utils.isAIMessage)(message)) {
            this.setAIMessageStatusToPending(message);
            return this.sendRequestToAICore(message)
        }
        const aiMessage = this.createPendingAIMessage(message);
        return this.sendRequestToAICore(aiMessage)
    }
    abortRequest() {
        var _this$aiAssistantInte2, _this$gridCommands5;
        null === (_this$aiAssistantInte2 = this.aiAssistantIntegrationController) || void 0 === _this$aiAssistantInte2 || _this$aiAssistantInte2.abortRequest();
        null === (_this$gridCommands5 = this.gridCommands) || void 0 === _this$gridCommands5 || _this$gridCommands5.abort()
    }
    isProcessing() {
        return this.processing
    }
    dispose() {
        var _this$aiAssistantInte3;
        super.dispose();
        null === (_this$aiAssistantInte3 = this.aiAssistantIntegrationController) || void 0 === _this$aiAssistantInte3 || _this$aiAssistantInte3.dispose()
    }
}
exports.AIAssistantController = AIAssistantController;
